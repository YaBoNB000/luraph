# Luau VM 混淆逆向攻防对抗复盘与技术评估报告

**对抗样本**：`ts.txt`（`local a = 25`）  
**防守方版本**：增量⑰（15 指令迷你 VM + 钥匙流四字节折叠 + 43 个动态加载 Handler + 反调试/防篡改守卫）  
**攻击方成果**：全链路脱壳、反序列化提取、VM 指令流反编译、原始代码完整还原  

---

## 第一部分：本轮复盘（`local a = 25` / 增量⑰ 复盘）

### Q1 & Q2. 迷你 VM 的真实处理路径与 15 指令特性评估

#### 1. 真实攻击处理手法：未写模拟器，直接借力宿主执行器
防守方在 Lua 源码层实现了一个 15 指令的迷你 VM（`while true do local cj=Ei[uw] ... end`）用于自解密元数据并拼接出核心代码生成器 `table.concat(lU)`。

在实际对抗中，**攻击方并未逆向其 ISA，也未编写专用的 Python 模拟器，而是直接利用宿主 Luau 解释器旁路穿透**：
- 宿主引擎本身就是最高效、最精准的虚拟机模拟器。
- 攻击方直接在迷你 VM 的输出汇聚点——即调用 `tA(table.concat(lU))` 的动态编译入口处注入 Hook 探针。
- 执行器仅花费不到 50ms 的预热时间，迷你 VM 就已自动在内存中完成全量状态机演算，完整解密并吐出 43 个 Handler 源代码及字节码解析器源码。

#### 2. 15 个指令特性的真实评估

| 特性分类 | 具体机制 | 威胁等级 | 攻击方实际感受与评估 |
| :--- | :--- | :--- | :--- |
| **纸老虎** | **每构建指令编号随机** | 0 星（完全无感） | 在执行器直接运行或局部仿真下，编号只是内部数组索引的分支映射，解释器自我寻址，对攻击方消耗为 0。 |
| **纸老虎** | **间接访存 / 双层解引用** | 0 星（完全无感） | 在宿主执行过程中仅为纳秒级内存查找，完全没有阻碍数据流汇聚。 |
| **纸老虎** | **JZ 相对跳转** | 0 星（完全无感） | 控制流即使做跳转，也是在内部状态机封闭运行，无法隐藏最终输出。 |
| **纸老虎** | **STP 自修改代码** | 0.5 星（微弱干扰） | 若采用纯静态 AST 匹配扫描会产生语法混淆，但在受控执行或符号模拟下，自修改不过是内存数组 `Ei[...]` 的常规赋值。 |

---

### Q3. 相比 R003 那轮，本轮新增成本来自哪里？增量⑰ 机制消耗排序

增量⑰ 确实增加了逆向工作量，本轮破解的时间开销分布如下（按实际消耗从高到低排序）：

```
[45%] 多层动态代码生成 (tA / LS 嵌套)
       └─ 拆分 43 个独立 Handler 密文，动态 LCG 解密并调用 loadstring，需精准定位二级编译下钩点。
[30%] 反调试与防篡改守卫 (wr11jz3vheAr)
       └─ 审计 debug.info / xpcall / newproxy canary，确认在原生 CLI 沙箱中是否触发死循环 Oo6yc()。
[20%] 外部状态机数据流分析 (Le / dX / Pl / YO)
       └─ 跟踪 58 个 32-bit 整数表与 5 段拼接逻辑，定位 223 字节 Payload 传递路径。
[ 5%] 四字节折叠消费 & 15 指令迷你 VM
       └─ 解释器黑盒自解密，几乎未造成有效阻碍。
```

---

### Q4. 四字节折叠消费（钥匙流）的技术评估与致命改进方案

#### 1. 现状评估
**毫无麻烦。**  
由于钥匙流算法采用的是确定性的伪随机数生成器（LCG/LFSR），且所有初始种子与状态均硬编码在文件内部（`b[76]`, `b[70]`, `b[112]` 等）。只要解释器闭包一启动，钥匙流便会自驱动流转至最终态。

#### 2. 能真正造成实质阻碍的改进方案
1. **指令与钥匙流交织（State Interleaving / JIT Decryption）**：
   - 严禁在初始化阶段一次性将全部 Payload 还原为静态字节码。
   - **第 $N+1$ 条指令的解密 Key，必须动态依赖第 $N$ 条指令执行后的寄存器状态或累积哈希**。
   - **效果**：攻击方无法通过单次 Dump 获取全量字节码，必须按真实路径单步步进；遇到条件分支时，静态遍历将面临路径爆炸（Path Explosion）。
2. **外部不可控熵绑定（Keyed Execution）**：
   - 将钥匙流种子与外部业务入参（如 UserID、Token、环境变量）强绑定。在离线脱壳无有效参数输入时，解密出的字节码将全为非法指令。

---

## 第二部分：动态攻击（新战场与动态防御评估）

### Q5. 动态攻击路径与下钩点（Hook Points）优先级排序

在动态/半动态分析中，攻击下钩点的选择直接决定破解效率：

```
[优先级 1] VM 解释器核心派发循环 (OW Loop / qt[Op])
     │ 理由: 这是所有虚拟化保护的终点，指令操作数、寄存器值、跳转目标全在此处裸奔。
     ▼
[优先级 2] 动态编译/代码加载接口 (loadstring / tA / LS)
     │ 理由: 截获所有动态生成的 Handler 源码与核心解析器。
     ▼
[优先级 3] 字节码反序列化器出口 (kz = parse(WM))
     │ 理由: 一网打尽所有 Prototypes、Constants、Slot 映射与 Instructions。
     ▼
[优先级 4] 常量池读取点 (C[k])
       理由: 价值较低，脱离控制流的孤立常量无法还原完整逻辑。
```

---

### Q6. 现有反调试/环境检测守卫的逐项评估与绕过方式

| 防御机制 | 威胁等级 | 攻击方真实感受与绕过手段 |
| :--- | :--- | :--- |
| **`debug.info` 的 `[C]` 探针** | **形同虚设** | 在标准 C 原生解释器中原生返回 `[C]`；在自定义测试容器中直接代理 C 包装函数即可伪造。 |
| **`newproxy` Metatable Canary** | **形同虚设** | 在静态分析与被动追踪中，只要不去主动触发非法元表调用或 `tostring`，该对象完全保持静默。 |
| **`_ENV` 污染 / `error = O5663`** | **微弱阻碍** | 触发时进入 `Oo6yc()` 死循环。绕过时只需在 AST 层面将 `LUlFoJmp = false` 写死，或将死循环函数置空。 |
| **行号一致性检查 (`debug.info(..., "l")`)** | **稍有阻碍** | 若格式化代码后运行会导致行号不匹配崩溃。**绕过方式**：以单行原始文本加载，或在 Hook 层全局代理 `debug.info` 返回静态伪造行号。 |

> **核心结论**：在 Lua 这种高度反射、无硬件环（Ring 0/Ring 3）隔离的脚本层，**纯 Lua 编写的反调试在掌控执行环境的攻击方眼中完全透明**。

---

### Q7. 动态防御机制有效性与资源浪费评级

```
┌────────────────────────────────────────────────────────────────────────┐
│ 必做：真正让攻击方难受的防御（大幅抬升逆向边际成本）                 │
├────────────────────────────────────────────────────────────────────────┤
│ 1. 运行时值绑定解码钥匙 (Keyed Execution)                             │
│    逻辑解密强依赖运行时外部参数，离线分析拿不到 Key 则无法脱壳。       │
│ 2. 指令级动态交织解密 (Just-In-Time Decryption)                       │
│    内存中永远不存在超过 1 条明文指令，彻底废掉静态 Dump 工具。         │
│ 3. 内存常量用后即焚 (Ephemeral Constants)                             │
│    常量加载至寄存器后立即将常量池或 Slot 置 nil，阻止后置内存抓取。    │
├────────────────────────────────────────────────────────────────────────┤
│ 选做：具有一定骚扰作用的中度防御                                      │
├────────────────────────────────────────────────────────────────────────┤
│ 4. 每次运行变异结构 (Polymorphic VM Context)                          │
│    打乱寄存器索引基址与上下文 table 偏移，破坏固定的 Hook 脚本。       │
├────────────────────────────────────────────────────────────────────────┤
│ 严禁浪费：投入产出极低的无效防御                                      │
├────────────────────────────────────────────────────────────────────────┤
│ 5. 代码段自检哈希 (Hash Integrity Check)                              │
│    攻击方既然能注入，就能 Hook 哈希计算函数返回预期的原值。            │
│ 6. 时序检测 (Timing Defense)                                          │
│    Lua GC 与宿主环境调度抖动极大，阈值过紧误杀，过松则被 Hook 完全无视。│
│ 7. Hook 框架特征检测 (Hook Detection)                                 │
│    陷入被动猫鼠游戏，攻击方使用原生闭包替换即可完美隐蔽。             │
└────────────────────────────────────────────────────────────────────────┘
```

---

### Q8. 第三方执行器环境下的安全边界与破解成本理论

**在第三方执行器（攻击方拥有完整内存读写、源码调试与函数 Hook 权）环境下，不存在无法攻破的软件防御。**

- **本质规律**：CPU/解释器要执行代码，就必然在某一时刻将逻辑还原为明文运算。
- **攻防本质**：混淆保护的目标不是建立“数学上的不可逆”，而是**将攻击方的边际成本从 5 分钟写脚本自动化脱壳，抬高到必须耗费数周单步分析跟踪**，使其破解的商业/时间价值归零。

---

## 第三部分：未来工程投资方向

### Q9. 静态与动态层面的最佳工程投资建议

#### 1. 静态层面最佳投资：【MBA 算术混淆 + 巨型超级指令（Super-operators / Handler Fusion）】
- **实施方案**：
  - 废除 `V[a+1] = C[b+1]` 这种单一语义的原子 Handler。
  - 将常见指令组合（如 `LoadK + Add + SetGlobal`）熔断为不可拆分的单一超大 Handler。
  - 在 Handler 内部大量注入 **MBA（混合布尔算术，如 $x + y = (x \oplus y) + 2(x \land y)$）** 并与 VM 状态强耦合。
- **打击效果**：使现有的 AST 模式匹配与反编译器完全失效，迫使攻击方必须引入昂贵的 Z3 SMT 求解器进行代数化简。

#### 2. 动态层面最佳投资：【Basic Block 级按需 JIT 变异 + 状态机死锁（State-Locked Bytecode）】
- **实施方案**：
  - 取消全局集中式反序列化，改为按基本块（Basic Block）动态解密。
  - 每个基本块末尾计算当前 VM 寄存器与状态签名，作为下一个基本块的解密 Key。
- **打击效果**：使集中式 `OW Loop Hook` 只能抓取到当前真实执行的一条路径；攻击方一旦尝试篡改寄存器探测其他分支，将直接导致后续字节码解密失败崩溃。

---

### Q10. 攻击方最忌惮的终极防御架构

> **攻击方最忌惮的防御架构：“无中心分派器的指令融合流（Decentralized Continuation-Passing VM）与业务输入强绑定。”**

**该架构具备以下特征**：
1. **无中心派发器**：废除集中式的 `while true do switch(op)` 循环，消灭全局下钩截断点。
2. **CPS 尾调用传递**：每个 Handler 执行完成后，利用混淆计算直接推导下一个 Handler 闭包并以尾调用（Tail Call）跳转。
3. **输入流强绑定**：解密状态机持续吸收业务输入参数作为动态密钥。

**对抗结果**：
- **静态上**：内存中无连续的 Bytecode 结构体可供读取。
- **动态上**：全局 Hook 点彻底消失，攻击方必须对分散在内存各处的尾调用链逐一插桩跟踪，逆向分析成本呈**指数级（Exponential）**激增。
