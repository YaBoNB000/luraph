# Luraph v0.2.0 混淆 Lua 脚本静态还原报告

对象: `cd.txt` (90,895 字节, 首行自报 `-- This file was protected using luraph v0.2.0`)
方法: 纯静态。全程未执行目标代码 —— 只用 Python 重放了其中的**纯算术**
(线性同余、XOR、base-94、varint), 等价于手工推演。

---

## 1. 攻击目标

还原这份被三层嵌套保护的 Luau (Roblox Lua) 脚本:

- **L0 外壳**: 控制流平坦化的状态机分发器 (~230 状态, 恒真谓词噪音);
- **L1 字符串/字节码层**: 5 段加密字符串, 拼成内层虚拟机的字节码容器;
- **L2 虚拟机层**: 自定义字节码解释器, 其**操作码处理函数本身也是加密的
  Lua 源码**, 运行时 `loadstring` 即席编译;
- 外加一整套反调试/反篡改/蜜罐逻辑。

最终目标: 恢复原始源码逻辑、全部常量、VM 指令语义、防御机制。

---

## 2. 逐步流程 (假设 → 操作 → 观察 → 结论)

**步骤 0 — 定性**
- 假设: 首行注释可信, 这是 Luraph。
- 操作: 通读顶层结构 `return setmetatable({大表},{}):eW()(...)`。
- 观察: 大表里混着 `coroutine.*`/`buffer.*`/`bit32`/`typeof`/`vector.create`、
  `+=`、`continue`、`local x = if ... then ... else ...`(if-表达式)。
- 结论: 目标是 **Roblox Luau** 脚本; 大表是一个"VM 环境对象", `eW` 是入口。

**步骤 1 — 找入口路径**
- 假设: `eW` 里初始化 `vN=4` 后循环调用分发器, 真实逻辑藏在少数"有副作用"的
  状态函数里, 其余是洗牌噪音。
- 操作: 写解析器把 `gX` 的嵌套 `if vN<=K` 判定树完整解析成二叉树, 按
  `vN<=K` 路由, 静态模拟执行; 每个处理函数的后继状态直接从其
  `return N, ...` 读出。
- 观察: 50 步路径唯一确定:
  `4:pR → 12:Cz → 15:Kq → 18:TV → 19:fe → 20:eG → …洗牌… → 225:[13] → 226:MI → 227:ya → 229:Yz → 232:kF`,
  其中 ~60 个函数只做参数置换(`return N, C, 置换后的参数`), 无副作用。
- 结论: 外壳可完全静态模拟; 有意义的事件只有: 算指纹、解 5 段字符串、
  构建内层 VM、两个完整性闸门、返回 VM 闭包。

**步骤 2 — 解 5 段字符串 (C[1..5])**
- 假设: `Cz..eG` 五个函数结构相同 = "从表 [16] 取连续字 → 重组字节 → 与
  某个密钥流 XOR", 且各自调用一次 `b[51]` 取密钥。
- 操作: 静态重放: 密文 `w=(T16[i]-(2798128065+i*308315633)) mod 2^32` 小端;
  `[51]` 是从种子 `b[22]=124835273` 出发的三连 LCG (mod 2^28);
  XOR 键 = `(kv+i) mod 256, i=1..len`。
- 观察: 解出 5 段共 **202 字节**, 内容含 `Y9dUk`/`YqKls` 等 5 字符转义序列;
  **字节总和 = 16993, 恰好等于 `pR` 算出的 C[8]** (程序自己用 `ya`/`Yz`
  做的闸门)。
- 结论: 5 段拼接 = 内层虚拟机字节码容器; 我的解密与程序的自检互相印证。

**步骤 3 — 解操作码处理函数 (VM 指令语义)**
- 假设: `[13]` 内的 `Rk{大字符串}` + `kP{三元组}` + `OJ` 装载循环 =
  "操作码 → 加密处理函数"映射表; 三元组语义为 (操作码, blob键, 明文长度)。
- 操作: 静态重放装载循环: blob 先 base-94 解码 (94 项字母表 `I`,
  5 字符→4 字节), 再按 `uw=(194014965+opc*120221117) mod 2^28`,
  **每字节**推进 `uw=(152091*uw+119909739) mod 2^28` 并用 `uw%256` 减密。
- 观察: 39/39 全部解成合法 Lua 源码, 统一形如
  `return function(E,a,b,c,d) local V,C,S,O,G,...=E[1]..E[22] <语义> end`;
  操作码集合 = 0..42 恰好缺 {15,19,23,31}。
- 结论: 拿到完整指令集; 缺失的 4 个操作码在内层解释器 `cS` 里内联实现,
  全部是 CALL 家族 (用 `V` 置换表算出字节值 19/15/23/31 后与缺失集合精确对上)。

**步骤 4 — 解内层字节码容器**
- 假设: `MI` 把 5 段拼成串交给 `dx`; `dx` 内 `M()` 做转义还原+base-94,
  `jo()` 再做一层流密码, 然后分段解析。
- 操作: 逐级重放。第一遍常量解析失败(值荒谬、块尾不闭合)。
- 观察: 重读源码发现常量块的**类型字节是明文读取**, 只有负载字节走 LCG ——
  我先前把类型字节也解了。修正后: 6 个常量干净解出, 43 字节恰好耗尽,
  **且指令流校验和 = 411152, 与硬编码 `NU={411152}` 完全一致**。
- 结论: 容器格式完全还原; 程序本体只有 6 条指令。

**步骤 5 — 反汇编与源码重建**
- 操作: 按 `cS` 的取值映射 (a=SD流, b=SC流, c=SA流, d=SB流, 寄存器经
  S=[8,9,4] 重映射) 逐条解释。
- 观察: `V[9]=_G["print"]; V[4]=12; V[4]=V[9](V[4]); V[8]=V[4]; return`。
- 结论: **原始源码就是 `print(12)`**。其余 (4 个未用常量、第 6 条垃圾指令、
  运行时把 op14 改写成 op42) 全是保护层与诱饵。

---

## 3. 依赖的特征 (突破口签名清单) ★

按发现顺序, 全是我实际用到的:

1. **自报家门**: 首行 `luraph v0.2.0` 注释 + 表内品牌串 `dL="LPH:"`
   → 直接定性混淆器家族, 省下盲目逆向。
2. **`setmetatable({...},{}):eW()(...)` 顶层形态** → 认出"大表即 VM 环境对象,
   入口函数返回真正的载荷闭包"这一 Luraph 经典骨架。
3. **`if vN<=K then … else …` 二叉判定树 + `vN,Ad,...=b:X(...);continue`**
   → 认出控制流平坦化分发器; 恒真谓词 (`vN-vN==0`、`vN*vN>=0`、
   `vN%2==0 or vN%2~=0` 及 `not`) 让我能把每个叶子坍缩成唯一真实跳转,
   从而纯静态模拟整个外壳。
4. **5 个孪生解密函数** (`Cz/Kq/TV/fe/eG`): 结构逐字相同, 仅
   `for wi=A,B`、`string.sub(...,1,L)`、目标槽 `C[n]=table.concat(o)`
   三处参数不同 → 一眼认出是"同一算法切 5 段", 且从表 `[16]` 的
   52 个字与 11+11+10+10+10=52 的切分完全吻合得到确证。
5. **大数字表 `[16]` (52×32位)** + 紧邻的 `[22]=124835273` 单值种子 →
   猜中"表是密文、单值是 PRNG 种子"; 三元 LCG 常数链
   `(414251,29440879)/(933843,97174573)/(526153,214486619)` 都在 2^28 模下。
6. **自洽指纹**: `pR` 计算 `0*31+16993=16993`, 而 `ya` 把解密结果的字节和
   存入 C[9], `Yz` 强制 `C[9]==C[8]` → 我把它当成**解密正确性的校验器**:
   解出的 202 字节和恰为 16993, 一次性确认整条链。
7. **`Rk{39 个 base94 风格大块} + kP{117 个整数的三元组} + 装载循环`** →
   认出"操作码处理函数以加密源码分发"。关键签名: 装载后立刻
   `loadstring(sub(x,1,len))()` —— 说明密文解开必然是**可读 Lua 源码**,
   这给了我一个硬判据 (解不出 ASCII 即算法有误)。
8. **base-94 字母表 `I[byte]=0..93`** (恰好 94 项、值域 0..93) +
   `Y+4字符` 转义表 `XH` → 容器编码直接反推。
9. **操作码键集合 0..42 恰好缺 {15,19,23,31}** → 推断这 4 个是内联实现的
   指令; 再算 `V` 置换表得 `V[40]=19,V[27]=15,V[29]=23,V[39]=31`, 与
   `cS` 内联分支 `if wS==V[40]…` 完全咬合 → 指令集闭合。
10. **指令采用 struct-of-arrays** (一条操作码流 + 四条操作数流) 且
    **校验和硬编码 `NU={411152}`** → 用校验和验证我的 varint/流解析逐字节正确。
11. **常量块"明文类型字节 + LCG 负载"** 的混合设计 → 第一遍全解密导致
    荒谬数值, 重读后修正; 修正后 43 字节精确耗尽 = 又一重确认。
12. **`Cl[6]=x` 运行时自修改** (`x=(245+53)%256=42`) → 认出静态/动态
    指令不一致的反静态技巧 (两条都是垃圾指令, 无实际影响)。
13. **`while true do end` 死循环陷阱** + `":(%d+):"` traceback 行号模式 +
    `"[C]"` 探针 + 三个随机蜜罐串 (`tddxhpfcpi`/`lsphhfstdm`/`vymgglkhqr`)
    → 逐项认出反调试/反篡改套件。
14. **大量零引用数据** (巨串 `BZ`、9 数表 `h`、`buffer.*` 导入表、
    垃圾函数 `[19]/[64]/[702]/[703]`) → 判定为诱饵/残留并剔除, 避免被带偏。

---

## 4. 实际还原出的东西

### 4.1 最终还原的原始源码
```lua
print(12)
```
(全部行为 = 调用全局 `print`, 参数 `12`; 主函数无参、无可变参、返回空)

### 4.2 程序常量表
| 位置 | 值 | 状态 |
|---|---|---|
| K1 | `659975.0` | 诱饵(未引用) |
| K2 | `12.0` | **使用** (print 的参数) |
| K3 | `"print"` | **使用** (全局名) |
| K4 | `nil` | 诱饵 |
| K5 | `nil` | 诱饵 |
| K6 | `581310.0` | 诱饵 |
| 种子 | `b[22]=124835273` | PRNG [51] 初值 |
| 指纹 | `16993` | C[8] = 字节码字节和 |
| 校验 | `411152` | 指令流校验和 (验证通过) |
| m_seed | `22229` | 常量块解密密钥种子 |

### 4.3 还原出的字节码 (主函数, 6 条指令)
```
pc  op  助记        语义
1   17  GETGLOBAL   V[9] = _G["print"]
2   37  LOADK       V[4] = 12
3   23  CALL        V[4] = V[9](V[4])      ; print(12), 1 参 1 返回
4   10  MOV         V[8] = V[4]
5   16  RETURN      返回 0 个值
6   14→42 JUNK      静态=op14, 运行时 patch 为 op42 (均为 local _=a*c+b)
```

### 4.4 还原出的指令语义 (43 条全集)
见 `analysis_summary.txt` D/E 节; 39 个处理函数的**原始 Lua 源码**已全部解出,
存于 `handlers/op00.lua … op42.lua` (缺 15/19/23/31 —— 那 4 条在解释器内联,
分别是: 19=多返回调用, 15=调用并追加结果到列表, 23=定长返回调用,
31=调用并记录返回个数)。代表性样例:

```lua
-- op17 GETGLOBAL
V[a + 1] = G[C[b + 1]]
-- op37 LOADK
local t = C[b + 1]; V[a + 1] = t
-- op32 GETTABLE (含 __index 元方法)
local t = V[b + 1]; local k = V[c + 1]; local r
if TYP(t) == MS[15] then r = RGET(t, k); if r == nil then <__index> end ...
-- op24 LOADSTR (短字符串直接编码在操作数里)
local ms = (63371 * a + 41221) % 65536
local q1 = (b - ms) % 65536 ... V[a + 1] = s
```

### 4.5 还原出的防御逻辑 (要点)
- **启动序言反调试** (失败即崩溃/挂起): 检查
  `type/pcall/xpcall/error/rawget/rawset/getmetatable/setmetatable` 均为
  function; `getfenv(0)` 为 table; `print`/`warn` 未被替换;
  `debug.info(fn,"l")` 返回的行号必须与从错误回溯用 `":(%d+):"` 提取的行号
  一致; `debug.info(error|loadstring|load,"s")` 必须为 `"[C]"`
  (检测被 Lua 重实现/挂钩的加载函数)。
- **蜜罐**: `newproxy` 用户数据与 3 个带毒元表对象
  (`tddxhpfcpi`/`lsphhfstdm`/`vymgglkhqr`), 任何 `__tostring/__concat/__call/
  __iter` 触发即置标志并崩溃; 引用被保留防 GC。
- **完整性闸门** (全用 `while true do end` 挂起): 字节码校验和≠411152、
  常量块未精确耗尽、`debug.info(loadstring,"s")~="[C]"`、解密字节和≠16993。
- **反静态**: 运行时自修改指令; 操作码处理函数加密分发即席编译; 诱饵常量。

### 4.6 交付物
- `analysis_summary.txt` —— 全部细节 (常量表/指令集/防御清单)
- `handlers/op00.lua … op42.lua` —— 39 个操作码处理函数原始源码
- `program.json` —— 反汇编结构化数据
- `stage1.py / stage2b.py / stage3.py / stage4.py` —— 本次静态重放脚本(可复现)

---

## 5. 卡在哪

1. **外层状态机的"洗牌"语义没有逐函数还原**。那 ~60 个 `return N, C, 置换`
   函数我只证明了"无副作用、可安全跳过", 没有逐个给出参数置换的精确映射 ——
   因为它们不影响任何可观察状态, 还原它们没有收益。
2. **内层字节码只有 6 条指令**, 所以 `CLOSURE/GETUPVAL/JMP/算术` 等大部分
   指令的语义虽已从处理函数源码还原, 却**没有真实样本佐证**(只能靠读源码
   推断, 置信度略低于被实际执行到的 17/37/23/10/16)。
3. **`BZ` 巨串、`h` 表等死数据**我没有深究其历史用途 (零引用, 判定为
   诱饵/版本残留后放弃)。
4. **`eW` 序言里 `yiunh()` 的精确触发时序**(哪些检查先挂起、哪些先崩溃)
   只做了定性描述, 未做完整控制流图。
5. 一处真正的弯路: 常量块类型字节误当密文解, 导致数值荒谬 —— 靠
   "块必须精确耗尽 + 校验和" 两个硬约束定位并修正。

---

## 6. 自评

- **信心**:
  - 原始源码 = `print(12)`: **确定还原** (校验和 411152 命中 + 字节和 16993
    命中 + 常量/指令全部自洽, 三重独立证据)。
  - 完整指令集 (43 条): **确定还原** (39 条直接解出源码, 4 条内联逻辑读自
    解释器本体)。
  - 外壳执行路径与全部防御机制: **高置信** (基于源码直读, 未运行验证)。
  - 死数据的"用途": 属于**合理推断** (零引用 ⇒ 诱饵/残留)。
- **步数**: 约 6 步 (定性 → 外壳模拟 → 字符串层 → 处理函数层 → 字节码容器
  → 反汇编), 中途 1 次实质性返工 (常量类型字节)。
- **最关键的三个特征**: ①孪生解密函数 + 表 [16]/种子 [22] 的切分吻合;
  ②自洽指纹 16993 与硬编码校验和 411152 (把我的人工解密和程序自检绑在一起);
  ③操作码键集合 0..42 缺 {15,19,23,31} 与内联 CALL 家族的精确咬合。

**如实声明**: 全程未运行目标脚本; 所有结论来自对代码的静态重放。唯一
"近似执行"的是我用 Python 独立实现了其中的纯算术 —— 这与运行原脚本无关,
且恰好被脚本自带的两道完整性闸门交叉验证。
