# 静态逆向分析报告 — `jjjk.txt`

**方法：** 100% 纯静态分析。脚本**从未被执行**、从未被载入任何 Lua 解释器，也没有获得源码或
混淆器名称。下文所有常量均通过阅读文件、并用 Python 手工复现其算术得到。

**结论：** **完全破解。** VM 本体、完整的 43 条指令集、反篡改状态机、以及被保护的载荷全部还原。
载荷是一个 **6 条指令、单函数原型的 Luau 程序，其全部内容就是 `print(12)`**。

---

## 1. 攻击目标

| 属性 | 值 |
|---|---|
| 文件 | `/home/user/uploads/jjjk.txt` |
| 大小 | 102,756 字节，**2 行**（102,755 个代码字符 + 结尾换行） |
| 方言 | **Luau**（Roblox Lua）— `if` 表达式、`+=`、`continue`、`type(x)` 快速调用、`newproxy`、`debug.info`、`bit32`、`buffer`、`task` |
| 入口 | `setmetatable({...206 个条目...},{}):t()(...)` |
| 保护类型 | **自研寄存器式字节码 VM**，含自解密 handler blob、LCG 密钥化 base-94 编解码、多阶段完整性状态机 |
| 推测来源 | Luraph 系 / "Luau VM" 类混淆器，面向 Roblox 第三方执行器 |

整条语句的含义是：*构造一张巨型查找表 `b`，运行其反篡改例程 `t`；`t` 解密并实例化一个字节码 VM；
VM 再运行唯一一个被保护的函数。*

---

## 2. 分步过程

每一步按 **假设 → 行动 → 观察 → 结论** 记录。

### 第 1 步 — 侦察
- **假设：** 整个文件是一个自执行表达式。
- **行动：** 读取首尾各 3,000 字符；做字节频率直方图。
- **观察：** 100% 可打印 ASCII，无二进制、无 base64。开头是
  `setmetatable({["..."]=..., [1]=buffer.writeu8, ...},{}):t()(...)`；
  结尾是一串 `pB[NNN]=[[ ... ]]` 赋值和 `while true do end` 循环。
- **结论：** 这是*源码级*混淆器（字符串/表编码 + VM），不是字节码转储。所有内容都必须符号化还原。

### 第 2 步 — 定位加密载荷
- **假设：** `pB[...]` 长括号字符串就是加密块。
- **行动：** 用 `pB\[(\d+)\]=\[(=*)\[` 正则扫描，并寻找配对的 `]=*]`。
- **观察：** **43 个 blob**，编号 0…42。另有一个 50 元素数组
  `[103]={4081784291, 981072594, ...}` 和一个 9 元素数组 `h={24123,237879608,...}`。
- **结论：** 43 个 blob ≈ 43 个 VM 操作码 handler。`[103]` 是一个 200 字节的 uint32 池。

### 第 3 步 — 编写 Luau 感知的分词器
- **假设：** 花括号配平可以隔离出每个顶层表条目。
- **行动：** 编写 `work/luatok.py`：关键字感知的 Lua/Luau 词法分析器 + `block_end()`（v1）与
  `block_end2()`（v2，处理 Luau `if` 表达式和 `+=`）。
- **观察：** v1 正确提取出 `[60]=function(...)` → **54,457 字符**（`vm60.lua`）；
  v2 却把它截断在 5,405 字符。
- **结论：** 两个都保留：v1 用于 VM 构建器，v2 用于外层表。共提取出 **206 个顶层条目** → `outer2.json`。

### 第 4 步 — 识别 VM 构建器
- **假设：** `[60]` 是混淆器的核心。
- **行动：** 通读整个 `vm60.lua`，找到 `local function oS(V)`。
- **观察：** `oS` 内部含：`k[1..46]` 计算常量、`MH()` varint 读取器、`m(au)` blob 解码器、
  `s(v,hA)` 原型解密器、`nP(...)` 闭包工厂、`XP(...)` 派发循环、`cS[0..42]` handler 表、
  `hk` 上下文、`OX={...}` 校验和、`T={V}` blob 列表。
- **结论：** 典型 VM 结构：`T` = 加密原型，`cS` = 操作码 handler，`XP` = 解释器。

### 第 5 步 — 破解数值常量表 `k`
- **假设：** `k[n]` 条目都是字面量上的算术表达式。
- **行动：** 用 Python 逐个求值全部 46 项 → `ktable.json`。
- **观察：** 全部解析为小整数。例如 base-94 字母表种子 `n₀=(206426422+13×94)%2²⁸`。
- **结论：** 混淆器把*每一个*魔数都藏在形如 `(A*B + C) % 2^28` 的两项 LCG 表达式后面。
  **这是最重要的总体特征。**

### 第 6 步 — 重建 base-94 字母表 `K`（不执行代码）
- **假设：** `K` 由 LCG 生成，因此完全由常量决定。
- **行动：** 实现生成器：`n₀=(206426422+13*94)%2²⁸`，
  `A=(131354713*15563+134564252)%2²⁸`，`B=(233076951*3803+116629634)%2²⁸`，
  `nᵢ₊₁=(A*nᵢ+B)%2²⁸`，字节 = `nᵢ%94+32` …（跳过冲突）。
- **观察：** 94 个字节 = **除 `Y`(89) 之外的全部可打印 ASCII 32–126**。
- **结论：** `Y` 被保留作**转义前导符**。与外层作用域中的字面表 `qU` 交叉验证 — 完全一致。

### 第 7 步 — 重建 `Y` 转义表
- **假设：** `qU` 把 5 字符的 `Y…` 序列映射到单字符。
- **行动：** 运行第二个 LCG（`J₀=(4135616+123281628)%2²⁸`，`A₂=(149160417*1653+133826964)%2²⁸`，
  `B₂=(213864738+103425519)%2²⁸`）。
- **观察：** 10 条转义：
  `YF2pQ→"`  `Y9dUk→'`  `YEPED→%`  `YqKls→␣`  `Ym75y→$`  `YOiTX→!`  `YQHFh→~`
  `Y62CR→#`  `Ybt7z→}`  `Y1nQc→&`
- **结论：** 编解码器完整还原：*反转义 Y 序列 → base-94（5 字符 → 4 字节小端）→
  4 字节小端长度前缀 → 载荷*。

### 第 8 步 — 重建操作码乱序表 `XF`
- **假设：** `XF[a]` 也是一个闭式 LCG。
- **行动：** `XF[a] = (l[a+1] - ((50240121+218256893)%2²⁸ + a*((250375483+18095145)%2²⁸)) % 65536) % 256`。
- **观察：** 一个 42 项的置换。特别地 `XF[40]=19, XF[39]=31, XF[29]=23, XF[27]=15` —
  这四个操作码由 `XP` **内联**处理，而不走 `cS`。
- **结论：** 该表既随机化操作码，又用于挑选解释器快速路径。

### 第 9 步 — 解密 43 个 handler blob
- **假设：** 每个 `pB[i]` 通过同一编解码器加上一个按索引密钥化的 LCG XOR 解出 Lua 源码。
- **行动：** `decode_handlers.py` — 层级感知的 blob 提取，然后
  `seed=((209738677+226*#wX)%2²⁸ + i*(190811360+27190211)%2²⁸)`，
  `mult=(160382102+115164327)%2²⁸`，`inc=(70270665+154310012)%2²⁸`，模 2²⁸，字节 = `state%256`。
- **观察：** **全部 43 个都解出 100% 可读的 Lua**（`handlers/op00.lua`…`op42.lua`）。每个都以
  `return function(E,a,b,c,d) local V,C,S,O,G,vargs,... = E[1],E[2],... end` 开头。
- **结论：** **完整的 VM 指令集及其全部语义已经掌握。**

### 第 10 步 — 解密状态机（`jt`）控制流
- **假设：** `jt` 是一棵覆盖约 244 个状态的二叉搜索派发树，由不透明谓词保护。
- **行动：** `parse_jt.py` — 递归下降遍历比较树，符号化求值每一个不透明谓词
  （`x*x>=0`、`(a*a+a)%2==0`、`b:XX(...)` 等）。
- **观察：** 244 个状态解析为一条 **118 状态的线性链**。其中约 109 个是纯参数置换空操作。
  真正干活的只有 9 个 handler：`KS, Gt, LR, lQ, AJ, AU, [60], Sy, QO, Ii, HX`。
- **结论：** 混淆器把 11 个真实步骤膨胀成 118 个状态 — 纯粹的反分析涂抹。

### 第 11 步 — **拦路虎：模数错误**
- **假设：** `C[1..5]`（被拆成 5 段的 VM blob）是从 `[103]` uint32 数组中减去一个算术密钥流、
  再与 PRNG 异或得到的。
- **行动：** 实现 `Op=2750890725`、`Ro=742309607`、
  `X = (Op + ctr*Ro) % 2³²`，小端拆 4 字节，取前 37–38 字节，与 `(key+i)%256` 异或。
- **观察：** 字节和 = **15017**，而反篡改目标 `C[8]` = **14715**。C[3] 和 C[5] 在任何密钥下
  都产生**零个**可打印解。穷举所有索引基（1–41）、计数器基（0–60）、密钥（0–255）后发现
  只有 `idxbase ∈ {1,11,31}` 曾经可行。
- **结论：** **找到 bug：** 我把 32 位模数写成了 `% 0xFFFFFFFF`（= 4 294 967 295），
  而正确值是 `% 4294967296`（= 2³²）。误差在 `ctr*Ro` 中不断累积。

### 第 12 步 — 解出 VM blob（突破）
- **行动：** 修正模数，重新解码。
- **观察：** **5 段全部 100% 可打印。字节和 = 14 715 — 与 `C[8]` 精确相符。**
  186 字节保存到 `vmblob.txt`。
- **结论：** 整条 C[1..5] 流水线获得独立的密码学级确认。

### 第 13 步 — 复现原型解析器 `s(v,hA)`
- **假设：** `s` 是一个两阶段分段解析器。
- **行动：** 精确移植：阶段 1 = LCG 解密（`seed = SM+(hA-1)*L`，`mult=Ik`，`inc=gL`，模 2²⁸，
  减去 `state%256`），然后读取带标签的段；阶段 2 = 在常量池上跑第二个 LCG（`IC*zu + wI` 模 2²⁸）。
- **观察：** 段标签 **118**=头部，**221**=常量种子，**237**=上值来源，
  **86**=常量池（`n`、`len`、体），**82**=操作数间接表 `S`，**230**=跳过，
  *其它* = 指令块（`n`、操作码字节，然后 4 个 varint 数组 `SA/SB/SC/SD`，最后是滚动校验和）。
- **结论：** 解析器能精确复现校验和。

### 第 14 步 — 解码主原型
- **行动：** `parse_proto(open('vmblob.txt'))`。
- **观察：**
  ```
  unescaped=150  base94→120 字节  声明长度=113  体=113
  checksum ck = 411152   ;  OX[1] = 567147260680+25400294411-592547143939 = 411152   ✔ 精确
  常量池消耗 43/43 字节 ✔   剩余字节 = 0 ✔
  nregs=3  nparams=0  vararg=0  upsrc=[]  ninst=6  nconst=6
  S = [8, 9, 4]
  C = {1: 659975.0, 2: 12.0, 3: "print", 4: nil, 5: nil, 6: 581310.0}
  ```
- **结论：** 三个独立校验和全部吻合。解码可证明正确。

### 第 15 步 — 反汇编
- **行动：** 映射操作数（`a=SD, b=SC, c=SA, d=SB`），并通过间接表 `S` 映射寄存器。
- **观察：** 见 §4.2。共 6 条指令；最后一条在加载时被改写（`Nn[6]=E`，`E=42`）。
- **结论：** **被保护的程序就是 `print(12)`。**

### 第 16 步 — 梳理反篡改体系
- **行动：** 提取 `t=function(b,...)`、`QO`、`Ii`、`HX`、`Sy`、`KS`、`[65]`。
- **观察：** 13 类不同防御（见 §3.5）。
- **结论：** 它们全都是*完整性/环境*检查，没有一个保护载荷的语义 — 这正是纯静态还原得以成功的原因。

---

## 3. 依赖的关键特征与指纹（详尽）

### 3.1 结构特征
1. **`setmetatable({ ... },{}):t()(...)` 单表达式外壳。** 整个程序是一条语句；
   `:t()` 跑完整性套件并返回一个 thunk，`(...)` 执行它。
2. **206 条目的扁平派发表 `b`。** 混合命名键（`jt`、`wH`、`CN`、`Sy`、`KS`、`QO`、`Ii`、`HX`、
   `Gt`、`LR`、`lQ`、`AJ`、`AU`、`jU`、`eJ`、`Hd`、`RX`、`wM`、`[60]` 等）与数值键。
   **实际只有 13 个数值键被引用过**：`1, 9, 36, 38, 46, 56, 60, 65, 83, 84, 103, 112, 117`。
   其余 70 个是诱饵。
3. **间接表 `J={[4]="wH",[7]="wH"}`**，使 `b[J[4]]` 对 grep 隐藏派发名。
4. **死 API 表面：** `[11]=buffer.writeu8`、`[3]=setfenv`、`[98]=coroutine.isyieldable`、
   `[16]=string.find`、`[20]=setmetatable`、`[1]=type`、`[9]=string.char`、`[36]=string.byte`。
   它们撑大表体积并暗示载荷从不使用的能力。
   **引用普查**（对全文 grep `b.NAME` / `b[NN]` / `b:NAME`，再与已定义键集合求差）：
   **83 个数值键中 70 个从未被引用；123 个命名键中 13 个从未被引用**
   （`AL, BL, EL, Hd, PL, QL, RX, Yd, cL, gL, h, qU, s`）。
   `wH` 对朴素 grep 看似未用 — 它只通过 `J={[4]="wH",[7]="wH"}` 间接到达。

### 3.2 算术混淆特征（最有用的一条）
5. **每个魔数都是一个两项 LCG 表达式** `(A*B + C) % 268435456`（2²⁸），
   或两个大字面量之差（`3726624979-90591637`）。文件中没有任何常量以明文形式出现。
   认出这个模式，我才能离线求出整套密码学参数。
6. **32 位乘法模拟** — `jU(b,u,z)` 用 `bit32.band/lshift/rshift` 对 16 位半字重新实现
   `u*z mod 2³²`。凡是混淆器不信任 Lua 浮点乘法的地方都用它。
   发现这一点让我确认全部算术是精确 32 位整数，而非浮点。
7. **`eJ(b,b) = b % 4294967296`** — 一个 2³² 归约器。⚠️ **把它抄成 `0xFFFFFFFF`（2³²−1）
   是那个阻塞整个分析数小时的唯一 bug。**

### 3.3 编解码器特征
8. **base-94 字母表 = 可打印 ASCII 32–126 去掉 `Y`。** 它是从 LCG *算出来*的，而不是读字面表 —
   所以它按构建随机化，但生成器常量是静态的。
9. **`Y` 是 5 字符转义的前导符。** 10 条转义覆盖 `" ' % ␣ $ ! ~ # } &` —
   恰好是那些会破坏 Lua 长括号或格式化字符串的字符。
10. **5 字符 → 4 字节，小端，`b = b*94 + K[c]`。**
11. **解码缓冲区内有 4 字节小端长度前缀**（`B[0] | B[1]<<8 | B[2]<<16 | B[3]<<24`），载荷紧随其后。
12. **`Yd`（10,506 字符）** 看起来完全像载荷 — 但其长度**不是** 5 的倍数，
    **不含任何 `Y`**，解码后约 38% 可打印（即均匀噪声），且**从未被引用**。纯诱饵。
    同类还有 `h={9 个 uint32}`、`PL="?"`、`EL=": "`、`AL="n"`、`BL="__index"`、`cL="string"`、
    `s={}`、`QL=false`、`qU`（转义表存在但内部会重新生成）、`Hd`、`RX`。

### 3.4 VM 特征
13. **`pB[i] = [[...]]` × 43** — 每个操作码一个加密 handler，启动时用 `loadstring` 载入并存入 `cS[i]`。
14. **handler 调用约定** — `cS[op](E, a, b, c, d)`，其中
    `a=SD[i], b=SC[i], c=SA[i], d=SB[i]`，且
    `E = {V, C, S, O, G, vargs, vargc, ups, makefn, mget, resolve_call, callcap,
    HAS_LEN_META, CHAR, FLOOR, ERR, TYP, GMT, RGET, RSET, Uu, MS, ln, lb}`（22 个槽 + 2 个临时）。
    识别出这个 22 槽签名后，每个 handler 都自解释了。
15. **稀疏寄存器堆 + 溢出槽。** `S[x]`（段标签 82）把操作数槽映射到真实寄存器；
    若 `S[x] == nil`，值存放在 `O[x]`（`Pa` 溢出表）中。
    这解释了为什么 `nregs=3` 却出现对 `R9` 的访问。
16. **`resolve_call(f)`** 返回 `fn, selfv` — `:` 方法调用语法糖通过 `selfv` 置位时把 `f`
    作为第 1 个参数前置来模拟。
17. **`callcap(fn, av, nargs)`** — pcall 式的带返回数上限的调用，返回 `out, nout`。
18. **四个操作码（15、19、23、31）是重复的**：一份在 `cS`，一份内联在 `XP` 的
    `XF[27]/XF[40]/XF[29]/XF[39]` 分支下。内联副本是快速路径，语义与解密出的 handler
    *逐字等价* — 这给了我对 handler 解密正确性的免费交叉验证。
19. **`HAS_LEN_META` 能力探测：**
    `mJ = F({},{__len=function() return 99 end}); fH = mJ==nil or false; … fH = wS and zE==99`。
    VM 在启动时探测宿主是否尊重 `__len`（Lua 5.2+ / Luau 对表），操作码 35 据此分支。
    这是*可移植性*探测，也是混淆器面向多种执行器环境的强指纹。
20. **内联短字符串操作码（24）。** `ms=(63371*a+41221)%65536`，然后 `q1=(b-ms)%65536`，
    `q2=(c-ms)%65536`，`q3=(d-ms)%65536`，`n=floor(q3/256)`，
    字符串是 `{q1%256, q1/256, q2%256, q2/256, q3%256}` 的前 `n` 个字节。
    ≤5 字符的字符串根本不进常量池 — 它们被夹带在操作数里。
    **这就是字节码中看不到任何可读字符串的原因。**
21. **运行时操作码改写：** 加载后 `local Nn=f[1].W; Nn[6]=E` 把第 6 条指令的操作码覆盖为
    `E = (229083711+39352043) % 2²⁸ % 256 = 42`。磁盘上的操作码（14）从来不是实际执行的那个。
    任何跳过这一步的反汇编器都会误解码尾部。

### 3.5 完整性 / 反分析特征
22. **`while true do end`** 作为通用篡改响应（5 处）。静默、不可杀，且**不产生任何消息**，
    因此动态分析只会卡死。
23. **`(nil)[1]`** 作为崩溃原语（`return(nil)[1]`），前面先 `_ENV = {error = EWkfWwm}`，
    使错误消息本身也被中和。
24. **`repeat ZcP+=1; _ENV={error=...} until ZcP==Oxm1_txdpMrcuk`**，其中
    `Oxm1_txdpMrcuk` 是 `nil` — 一个永不终止的循环。
25. **三重校验和链。**
    - `QO` 计算 `GW = C[1..5] 的字节和`（注意：C[3] 是**倒序**求和，`for i=#C[3],1,-1`，
      用来击败朴素的重实现），并设置 `C[9] = GW % 2³²`。
    - `KS` 从 `C[8]=0` 出发计算 `C[8] = (C[8]*31 + 14715) % 2³²` → `C[8] = 14715`（硬编码期望值）。
    - `Ii` 要求 `C[9] == C[8]`，否则 `while true do end`。
    因此**改动加密 VM blob 中任何一个字节都会让脚本挂死。**
26. **逐原型字节码校验和：** `if f[Mw].ck ~= OX[Mw] then while true do end end`，
    其中 `OX = {567147260680+25400294411-592547143939}`。校验和本身是四个操作数 varint 流的
    32 位滚动求和。
27. **常量池长度检查：** `s()` 内部的 `if P ~= Bd+FF then while true do end end`。
28. **字符串常量由 `string.char` 数组乱序拼装。** 在 `QO` 中：
    `Bkt={5:100,1:101,2:98,4:117,3:103}; Bko={5,1,2,4,3}` → `"debug"`；
    `iJt={4:105,1:110,2:102,3:111}; iJo={4,1,2,3}` → `"info"`。
    同样方式拼出 `"loadstring"`、`"load"`、`"error"`、`"[C]"`、`"function"`、`"s"`。
    *索引数组*与*值表*都乱序存放，所以字符串在原处不可读。
29. **`debug.info(fn,"s") == "[C]"` 挂钩检测**，针对 `error`、`loadstring`、`load`。
    若其中任何一个被替换为 Lua 层挂钩，其 source 就不再是 `"[C]"`，脚本立即自毁。
    （注意它用的是 **`debug.info`**，即 Luau API，而非 `debug.getinfo`。）
30. **行号一致性检查。** `C9R6` 调用 `pcall(debug.info, fs, "l")` 取得 `currentline`，
    然后 `xpcall(fs, handler)`，handler 用 `gmatch(":(%d+):")` 从错误字符串中抽出行号，
    并断言两者相等。可检测字节码被重新加载、chunk 被改名或行号信息被剥离。
31. **`newproxy(true)` 金丝雀。** `Uln50` 创建完整 userdata，取其元表，
    安装 `__tostring = __concat = __call = VPbR_dr14`（自毁函数）。共 4 个金丝雀对象
    返回在 `H1bv2iE_kr_4` 中。若沙箱缺失 `newproxy`，检查即失败。
32. **`__metatable` 尊重性检查。** `g9z0qid7drof7(tag)` 构造
    `{__metatable=tag, __tostring=VPbR_dr14, __concat=VPbR_dr14, __iter=VPbR_dr14}`，
    断言 `setmetatable` 返回该表，且 `getmetatable` 返回**标签字符串**（而非元表本身）。
    标签是无意义字符串 `"tdxdxhpfcpi"`、`"lsphhlfstdm"`、`"vymgglkhr"`。
33. **标准库同一性检查。** `NIoji5x` 断言
    `type(type)==type(pcall)==type(xpcall)==type(error)==type(rawget)==type(rawset)==
    type(getmetatable)==type(setmetatable)=="function"`。
    `Lk9` 还断言 `_G.print == print`、`_G.warn == warn`、`rawset/rawget` 在**负数键**上往返一致、
    置 `nil` 确实删除、以及 `pcall(error)` **必须失败**。
34. **`unpack`/`table.unpack` 能力探测：** `pcall(unpack, {}, 0, 64)` 必须成功。
35. **244 状态的 `jt` 状态机，含约 109 个空操作置换器。** 每个真实 handler 返回
    `(下一状态, C, 置换后的参数...)` — 每一跳的参数置换都不同，所以手工追踪某个值穿过状态机
    被刻意做得极其痛苦。
36. **不透明谓词。** `local Wu = if s>=0 then 12 else 127`、
    `local fk = C[8]~=nil and 27 or 217`、`local Mf = if C[8]>=0 then 63 else 215`、
    `Wu-Wu`、`+1-1`、`x*x>=0`。每一个都是静态常量；它们存在的唯一目的是让树遍历产生歧义。
37. **被禁用的第二阶段。**
    `local AZ = f[#T].ck % 1; while AZ>0 do … end` — `ck%1` 恒为 0，所以这整个自解释迷你 VM
    （按 `XF[23]`/`XF[28]` 派发）是死代码。它要么是残留，要么是给"修好它"的分析者准备的蜜罐。
38. **诱饵函数。** `[54]`、`[74]` 索引 `b[1][4]`（即 `type[4]`）并会报错；
    `[686]`、`[687]` 是使用不同常量的替代 PRNG，从不调用；`Bt` 和 `_k` 形状正确但从不被派发到。

### 3.6 环境指纹（目标识别）
39. `newproxy`、`debug.info`、`bit32`、`buffer.*`、`task`、`getfenv` 与 `_ENV` 同时存在、
    标准库同一性检查中包含 `warn` → **Roblox Luau**，且具体是**第三方执行器**环境
    （那些"loadstring/load 必须是 C 函数"的检查，只在一个执行器可能挂钩它们的场景下才有意义）。

---

## 4. 已还原的产物

所有产物位于 `/home/user/work/`。

### 4.1 文件清单
| 路径 | 内容 |
|---|---|
| `vmblob.txt` | **186 字节解码后的 VM blob**（`C[1]..C[5]`），字节和 14 715 ✔ |
| `disasm.txt` | **被保护程序的反汇编** |
| `proto.json` | 解析后的原型（全部段、常量、操作数） |
| `handlers/op00.lua` … `op42.lua` | **全部 43 个解密后的操作码 handler**（可读 Lua） |
| `cS.json` | 同上，按操作码索引 |
| `tables.json` | 计算出的 `K` 字母表、`Y` 转义、`XF` 映射、`O` 数组 |
| `jt_map.json` | 全部 244 个状态的完整 状态→handler 映射 |
| `outer2.json` | 全部 206 个外层表条目 |
| `vm60.lua` / `vm60_resolved.lua` | VM 构建器：原始版与把 `k[...]` 折叠为数字后的版本 |
| `ktable.json` | 46 项的 `k` 常量表 |
| `t_func.txt` | 完整的反篡改入口函数 |
| `s_func.lua` / `s_func_pretty.lua` | 原型解析器 |
| `vmdecode.py`、`decode_handlers.py`、`parse_jt.py`、`luatok.py` | **可运行的纯 Python 重实现** |

### 4.2 反汇编
```
; VM function prototype #1   nregs=3  nparams=0  vararg=0  ninst=6  nconst=6
; ck = 411152  (== OX[1], 已验证)   常量池 43/43 字节   剩余 0
; 操作数间接表 S = [8, 9, 4]
; 常量池:
;   C[1] = 659975.0     <- 诱饵
;   C[2] = 12.0
;   C[3] = "print"
;   C[4] = nil          <- 诱饵
;   C[5] = nil          <- 诱饵
;   C[6] = 581310.0     <- 诱饵

[001] GETGLOBAL   a=8      b=2      c=29222  d=62818    R9 = _G["print"]
[002] LOADK       a=3      b=1      c=35180  d=46739    R4 = 12.0
[003] CALL        a=1      b=1      c=1      d=0        R9(R4)   nargs=1 nres=1
[004] MOVE        a=7      b=3      c=23073  d=49105    R8 = R4          （死代码）
[005] RETURN      a=0      b=0      c=0      d=0        return  (0 个值)
[006] NOP         a=57104  b=29473  c=40696  d=37715    -- 不可达；加载时被改写为 op42
```
[001]、[002]、[004]、[006] 的 `c`/`d` 操作数都是很大的随机值 — 无用填充，属于进一步的涂抹。

### 4.3 还原出的原始源码
```lua
print(12)
```
这就是被保护载荷的全部内容。（指令 004 是寄存器分配器产生的死移动；指令 006 是死代码。）

### 4.4 完整 VM 指令集（43 条操作码）
操作数映射：`a=SD, b=SC, c=SA, d=SB`。`V`=寄存器，`C`=常量，`S`=操作数间接表，
`O`=溢出槽，`G`=`getfenv(0)` 全局环境，`ups`=上值。

| # | 助记符 | 语义 |
|---|---|---|
| 0 | `EQ` | `V[a+1] = (V[b+1] == V[c+1])`，尊重 `__eq` |
| 1 | `APPEND` | `V[a+1][V[b+1]+1] = V[c+1]; V[b+1] += 1` — 表构造器追加 |
| 2 | `NEWTABLE` | `V[a+1] = {}` |
| 3 | `LT` | `V[a+1] = (V[b+1] < V[c+1])`，尊重 `__lt` |
| 4 | `IDIV` | `V[a+1] = floor(V[b+1] / V[c+1])`（`//`） |
| 5 | `JMPF` | `if not V[a+1] then pc = b` |
| 6 | `VARARGC` | `V[a+1] = vargc` |
| 7 | `NOT` | `V[a+1] = not V[b+1]` |
| 8 | `MUL` | `V[a+1] = V[b+1] * V[c+1]`，尊重 `__mul` |
| 9 | `GE` | `V[a+1] = (V[b+1] >= V[c+1])`，通过反向 `__le` 实现 |
| 10 | `MOVE` | `V[a+1] = V[b+1]` |
| 11 | `NEQ` | `V[a+1] = not (V[b+1] == V[c+1])` |
| 12 | `SETUPVAL` | `ups[a+1].v[ups[a+1].i] = V[b+1]` |
| 13 | `POW` | `V[a+1] = V[b+1] ^ V[c+1]`，尊重 `__pow` |
| 14 | `NOP` | `local _ = a*c+b` — 垃圾指令 |
| 15 | `CALLT` | 方法调用；结果追加进表 `V[b+1]` 的索引 `V[c+1]` 处；`nfixed=floor(d/2)`，`tail = d%2==1` |
| 16 | `RETURN` | `b==255` → 多值返回（`d` 个固定寄存器，然后若 `c==1` 接可变参数，否则通过 `E.lb/E.ln` 接上次调用的结果）；否则返回 `b` 个值 |
| 17 | `GETGLOBAL` | `V[a+1] = G[C[b+1]]` |
| 18 | `MOD` | `V[a+1] = V[b+1] % V[c+1]`，尊重 `__mod` |
| 19 | `CALL_V` | 完整调用：`nfixed=b`，`ntail=V[S[bs+b+1]]`，`d==1` 时接可变参数，`nres=c` |
| 20 | `GETUPVAL` | `V[a+1] = ups[b+1].v[ups[b+1].i]` |
| 21 | `SETLISTV` | 从索引 `V[b+1]` 起把 `vargs` 追加进表 `V[a+1]` |
| 22 | `CLRN` | `for i=1,b do V[S[a+i]] = nil end` |
| 23 | `CALL` | 简单调用：`nargs=b`，`nres=c`，`d==1` 时接可变参数，结果写入 `S[bs+i]`/`O[bs+i]` |
| 24 | `SHORTSTR` | 由 `a,b,c,d` 构造内联 ≤5 字符字符串，掩码为 `(63371*a+41221)%65536` |
| 25 | `SETTABLE` | `V[a+1][V[b+1]] = V[c+1]`，尊重 `__newindex` |
| 26 | `JMPT` | `if V[a+1] then pc = b` |
| 27 | `GT` | `V[a+1] = (V[b+1] > V[c+1])`，通过反向 `__lt` 实现 |
| 28 | `CLOSURE` | `V[a+1] = makefn(b+1, V, ups, S)` |
| 29 | `JMP` | `pc = b` |
| 30 | `UNM` | `V[a+1] = -V[b+1]`，尊重 `__unm` |
| 31 | `CALL_T` | 带尾部计数的调用：`varg = d%2==1`，`tail = d>=2`，把 `nout` 写回 `V[S[base]]` |
| 32 | `GETTABLE` | `V[a+1] = V[b+1][V[c+1]]`，尊重 `__index` |
| 33 | `VARARGT` | `V[a+1] = {vargs…}` |
| 34 | `DIV` | `V[a+1] = V[b+1] / V[c+1]`，尊重 `__div` |
| 35 | `LEN` | `V[a+1] = #V[b+1]`；若 `HAS_LEN_META` 则先尝试 `__len`（`MS[22]`） |
| 36 | `LE` | `V[a+1] = (V[b+1] <= V[c+1])`，尊重 `__le` |
| 37 | `LOADK` | `V[a+1] = C[b+1]` |
| 38 | `SUB` | `V[a+1] = V[b+1] - V[c+1]`，尊重 `__sub` |
| 39 | `CONCAT` | `V[a+1] = V[b+1] .. V[c+1]`，尊重 `__concat` |
| 40 | `ADD` | `V[a+1] = V[b+1] + V[c+1]`，尊重 `__add` |
| 41 | `SETGLOBAL` | `G[C[b+1]] = V[a+1]` |
| 42 | `NOP2` | `local _ = a*c+b` — 垃圾指令 |

常量池值标签：`0`=nil，`1`=bool，`3`=string（2 字节小端长度 + 原始字节），
*其它* = 浮点数，编码为 `尾数 × 2^(指数字段//2 − 2048)`，指数字段为奇数时取负。

### 4.5 混淆器的完整执行轨迹
```
setmetatable(b,{}):t()
 ├─ [立即执行函数 "hit"] 5 项飞行前检查（NIoji5x, Lk9, aXmgh2xzxj, C9R6, Uln50）
 │     └─ 失败时: VPbR_dr14 → j19__wLwu7 → _ENV={error=…}; repeat…until nil; return (nil)[1]
 ├─ b:CN()  → (true, 3)                     ; u=true，初始状态 = 3
 └─ while u do  h,B = b:wH(b, C, …)          ; jt 状态机，244 状态 / 118 步链
       ├─ Gt/LR/lQ/AJ/AU : 从 b[103] 读 10 个 uint32，减去 (2750890725 + ctr*742309607)%2³²，
       │                   小端拆分，取 37–38 字节，与 (key+i)%256 异或  →  C[1]…C[5]
       │                   密钥来自 b[65]（3 步 LCG 模 2²⁸，状态 b[83]=233844545，
       │                   首次调用加 b[112]=248940616）→ 96, 97, 72, 185, 176
       ├─ KS : C[8] = (C[8]*31 + 14715) % 2³²   → 14715
       ├─ [60] : 定义 oS(V) — VM 本体。断言 debug.info(loadstring,"s").source == "[C]"。
       ├─ Sy : C[6]=oS ; E = function() return C[6](C[1]..C[2]..C[3]..C[4]..C[5]) end
       │       C[10]=32 ; C[7] = function(...) return b[84](b, E, ...) end
       ├─ QO : GW = C[1..5] 的字节和（C[3] 倒序求和）; C[9] = GW % 2³²
       │       拼装 "debug","info","loadstring","load","error","[C]","function","s"
       │       断言 debug.info(error/loadstring/load,"s").source == "[C]"
       ├─ Ii : 要求 C[9] == C[8]，否则 while true do end
       └─ HX : return 2, C[7]      ; 驱动循环退出，t() 返回 C[7]
然后 (…)
 └─ C[7]() → b[84](b, E) → E() → oS(C[1]..C[2]..C[3]..C[4]..C[5])
      ├─ m(au) : 反转义 Y → base-94 → 4 字节小端长度 → 113 字节体
      ├─ 解密 43 个 pB blob → loadstring → cS[0..42]
      ├─ s(T[1],1) : LCG 解密（seed SM, mult Ik, inc gL, 模 2²⁸）→ 解析各段
      │              断言 ck == OX[1] (411152) ✔   断言 P == Bd+FF ✔
      ├─ f[1].W[6] = 42                 ; 运行时操作码改写
      ├─ AZ = f[#T].ck % 1 = 0          ; 死的第二阶段被跳过
      └─ XP(f[1], {}, {}, {}, 0)        ; 执行 6 条指令
            R9 = _G["print"];  R4 = 12.0;  print(12);  R8 = R4;  return
```

---

## 5. 遇到的阻碍

| # | 阻碍 | 严重度 | 解决方式 |
|---|---|---|---|
| 1 | **无 Lua/Luau 解释器**且禁止执行 | 高 | 用纯 Python 重实现编解码器、全部三个 LCG、varint 读取器、分段解析器和常量池解码器（`vmdecode.py`）。 |
| 2 | **Luau 专有语法**（`if` 表达式、`continue`、`+=`、`type(x)`）使所有现成 Lua 解析器失效 | 高 | 手写 `luatok.py`，关键字感知，配两套块匹配器。 |
| 3 | **`block_end2` 对 `[60]` 作用域判断错误**（5,405 字符而非 54,457）— Luau `if` 表达式检测问题 | 中 | `[60]` 保留 `block_end` v1，`block_end2` 只用于外层表。并记录了各自权威适用范围。 |
| 4 | **分词器死循环** — `;`、`{`、`[` 被错误地放进了 `EXPR_PREV` | 中 | 移除它们。 |
| 5 | **🔴 模数 bug。** 在 C[1..5] 密钥流中把 `% 4294967296`（2³²）写成了 `% 0xFFFFFFFF`（2³²−1） | **致命** — 耗时最长 | 由字节和不匹配（15017 vs 目标 14715）以及 C[3]/C[5] 在任何密钥下*零*可打印解发现。穷举所有索引基/计数器基/密钥后定位到累加器。修正后 → 5 段全部可打印，字节和**精确为 14715**。 |
| 6 | **blob 提取正则** `pB\[\d+\]=\[(=*)\[(.*?)\]=*\]` 在部分 blob 上错配 | 中 | 改为对精确的 `]=q]` 结束符做层级感知匹配。 |
| 7 | **诱饵数据**（`Yd` 10 506 字符、`h`、`[54]`、`[74]`、`[686]`、`[687]`、`Bt`、`_k`、70 个未用数值槽、`ck%1` 死阶段） | 中 | 用引用普查解决：对全文 grep `b.NAME` / `b[NN]` / `b:NAME`，与已定义键集合求差。证明 **83 个数值键中 70 个**、**123 个命名键中 13 个**从未被引用。`wH` 一开始看似未用 — 它只通过间接表 `J={[4]="wH",[7]="wH"}` 到达，朴素 grep 会漏掉。 |
| 8 | **运行时操作码改写**（`f[1].W[6] = 42`）意味着存储的字节码 ≠ 执行的字节码 | 低 | 通过阅读加载器发现；已记录在反汇编中。 |
| 9 | **稀疏寄存器堆** — `nregs=3` 但指令访问 `R9` | 低 | 从 handler 中读出 `S`/`O` 溢出语义后即理解。 |

**残留阻碍：对本文件而言，无。** 纯静态分析唯一无法回答的是：混淆器的*生成器*是否每次构建都
随机化 LCG 常量（我的工作假设是"是" — 依据是 `Yd` 是未被引用的噪声、`OX` 以未求值的算术表达式
形式写出）。因此这套 Python 工具链是**针对本样本**的，换一份构建需要重新提取常量。

---

## 6. 自我评估

### 置信度：载荷 **98 %**，VM **95 %**，完整性逻辑 **100 %**。

**为什么载荷还原基本确定。** 四项相互独立、我从未去调参的检查全部通过：

1. `QO` 对 `C[1..5]` 的字节和 = **14 715**，与 `KS` 由字面量 `14715` 产生的硬编码 `C[8]` 相符。
   这验证了整条 `b[103]` → 密钥流 → 异或 流水线。
2. `s()` 的逐原型校验和 `ck` = **411 152**，与 `OX[1]` =
   `567147260680+25400294411-592547143939` 相符。这验证了 LCG 解密*和* varint 操作数流。
3. 常量池恰好消耗 **43 / 43** 声明字节，满足 `if P ~= Bd+FF then while true do end` 守卫。
4. 分段解析消耗了 **113 / 113** 全部字节，**剩余 0**；base-94 层消耗了全部 150 个反转义字符。

一次错误的解码要同时巧合命中这四项是不可能的。而且它还产出了 `"print"` 和 `12.0`
这样带类型的常量 — 语义连贯，不是噪声。

**为什么 VM 还原很高但非绝对。** 全部 43 个 handler 都解出语法有效、语义连贯的 Lua，
且共享同一个 22 槽上下文签名；操作码 15/19/23/31 在 `XP` 内有逐字等价的内联副本，
与解密出的 handler 完全吻合 — 这是一次独立交叉验证。剩余 5 % 在于：部分 handler 助记符
（`APPEND`、`SETLISTV`、`CLRN`）是我从代码推断而非来自符号表；
操作数槽映射（`a=SD, b=SC, c=SA, d=SB`）我只针对那四条内联快速路径做了验证。

**我最不确定的地方。**
- 生成器是否每次构建随机化常量（很可能是 → 我的工具链是样本专用的）。
- `ck%1` 那个死的第二阶段是残留还是刻意禁用的功能。
- 对超出 `S` 范围的寄存器，`O`/`Pa` 溢出槽的确切语义 — 因为载荷从未触发它们。

**让我意外的地方。** 一个 102 KB、43 条操作码、三重校验、118 状态的 VM，配上 `newproxy` 金丝雀、
`debug.info` 挂钩检测和行号交叉验证 — 全部用来保护 `print(12)`。
保护与载荷的比例大约是 **100 000 : 1**。这是该混淆器的一个*演示/概念验证*构建，
或用于验证打包器的测试载荷；机器是生产级的，但内容不是。

### 可复现性
`work/` 下所有代码都是无依赖的纯 Python 3：
```
python3 vmdecode.py            # 编解码器 + 原型解析器（可作为模块导入）
python3 decode_handlers.py     # 重新生成 handlers/op00..op42.lua
python3 parse_jt.py            # 重新生成 jt_map.json
```
