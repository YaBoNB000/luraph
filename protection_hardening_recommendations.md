# Suda Luau VM 保护方案：加固建议

## 1. 执行摘要

本次验收显示：当前方案能有效增加人工阅读和一次性静态分析的成本，但其核心保护边界位于客户端进程内。载荷在执行前会依次成为普通 Lua/Luau string、table 和 closure 可访问的对象，因此具备源码改写能力的分析者可在稳定的汇合点观察到完整根原型、规范化指令列和明文常量。

最关键的实测观察点是：根原型 `qQ` 在传入 `bL(qQ,...)` 前完整存在；`BH(yP,...)` 返回后，常量池中已经以明文 Lua string 形式出现 `print` 与 `Suda`。当前反调试检查主要覆盖全局 API、环境和调用栈特征，没有覆盖闭包内部数据对象的可观察性。

因此建议的目标不是“让客户端代码绝对不可逆”（这在通用客户端上不成立），而是：

1. 减少高价值明文的范围、存活时间和可遍历性；
2. 让脱壳必须跨越多处动态状态，而不是命中一个稳定单点；
3. 将真正重要的决策、密钥和授权逻辑移出客户端；
4. 对篡改和异常执行环境提高检测概率，并将检测结果连接到服务端风险处置。

## 2. 已验证的薄弱点

| 优先级 | 实测现象 | 为什么会削弱保护 | 建议方向 |
|---|---|---|---|
| P0 | `qQ` 在 `bL(qQ,jC,{},vU,0)` 之前是完整 Lua table | 插入一个局部转储函数即可枚举根原型 | 不一次性 materialize 完整原型；采用页/块按需解码 |
| P0 | `BH` 后的常量池含明文 `print`、`Suda` | 字符串和全局符号名称直接暴露语义 | 常量按使用点解码，尽快覆盖；敏感语义外置 |
| P1 | 原型、指令列、常量均可被同一轮插桩获得 | 数据面汇合过于集中，提取链短 | 分离密钥派生、指令取数和常量解码的时机/位置 |
| P1 | 检测偏重 `debug`/`load`/环境特征 | 同一闭包内部插桩不必替换这些 API | 加入结构完整性与控制流完整性检查 |
| P2 | 大量平坦化和不透明算术不改变运行时数据形态 | 增加阅读成本，但不阻止动态观察 | 将混淆预算转向运行时绑定和服务端约束 |

## 3. P0：缩短和拆分解密后的对象生命周期

### 3.1 不要一次性构建完整原型树

当前模式可概括为：密文载荷 → 解压/反序列化 → 完整 `qQ` table → VM 执行。完整对象在 `bL` 前存在稳定窗口，适合枚举。

建议改为按基本块或指令页解码：

```text
加密页 + 页认证标签
        │
        ▼
当前 PC 对应页的临时解码缓冲
        │
        ▼
执行少量指令
        │
        ▼
覆盖缓冲 / 丢弃引用 / 派生下一页密钥
```

实现要点：

- 每个页只包含有限数量的指令与必要常量，避免一个 table 包含全量程序；
- 页密钥应由会话盐、页序号、前一页认证结果和服务端下发材料共同派生；
- 使用结束后清除数组元素并断开引用。Luau 垃圾回收不能保证安全擦除，但能缩小可观察窗口；
- 跳转、异常和闭包创建不能迫使提前解密整个 CFG；需要在编译期限制或拆分跨页依赖；
- 页认证失败必须进入可控失败路径，不能回退到明文或弱校验路径。

注意：按页解码主要增加动态转储的成本，不应被宣传为保密边界；攻击者仍可收集所有被执行页。

### 3.2 常量按需解码并立即失效

不要让常量池长期包含直接可读的字符串。对于字符串、表键、函数名和协议字段：

- 将常量切分或以编码片段存储；
- 在真正的消费点解码，而不是在原型初始化时解码；
- 在一次调用后删除临时 table entry、覆盖 byte array，并将下一次使用绑定到新鲜派生状态；
- 对非常敏感的常量，不在客户端下发可逆表示，改由服务端返回能力令牌或操作结果；
- 不要仅用固定 XOR/固定 PRNG。固定方案只是静态解码问题，无法提供真正的会话隔离。

实务上，`print` 这类无敏感性全局名称不值得为其付出大量性能成本；应优先保护业务规则、协议密钥、风控阈值、反作弊判定和高价值资源路径。

## 4. P0：服务端化真正的信任边界

任何必须秘密、必须可信或影响资产归属的逻辑，都不应以“客户端 VM 混淆”作为唯一控制措施。

建议按资产分级：

| 内容 | 客户端可保留 | 应移至服务端 |
|---|---|---|
| UI 展示、纯视觉计算 | 可以 | 不适用 |
| 非关键本地性能优化 | 可以，假定可被修改 | 服务端做结果合理性检查 |
| 经济结算、库存、权限 | 仅提交请求 | 最终判定、状态写入、审计 |
| 风控规则、作弊判定阈值 | 仅保留低价值预筛选 | 完整规则、模型和处置 |
| 长期密钥、签名私钥 | 不可保留 | HSM/KMS 或受控服务 |

客户端可以执行快速预检查，但服务器必须重新验证请求的身份、授权、重放、时效、上下文和状态机转换。

## 5. P1：把完整性校验覆盖到真实攻击路径

### 5.1 覆盖关键闭包和关键数据，而非只检查全局环境

当前检测若只验证 `_G`、`debug.info`、`loadstring` 等，容易被绕开：分析者可以不替换这些全局对象，而是在目标闭包内部增加观测语句。

建议建立多层完整性策略：

1. 构建期：为每个关键代码段、页、常量元数据生成独立完整性标签；
2. 启动期：验证装载器、关键 dispatch 函数和密钥派生材料；
3. 执行期：在不可预测的指令边界验证即将执行页、控制状态和前序结果；
4. 业务期：把本地风险信号、会话 nonce、版本和挑战响应提交服务端；
5. 服务端：按风险分层处理（记录、提高验证、限制高价值操作、阻断）。

不要将所有校验集中在启动阶段，否则攻击者只需通过一次启动后再插桩。也不要单独依赖源文本 hash：发布格式、解释器版本和合法热修复会使其脆弱。

### 5.2 绑定控制流状态与载荷认证

可让每个页的认证输入包含：会话 nonce、页号、前一页摘要、逻辑调用深度以及特定 VM 寄存器摘要。这样，离线抽取单页后再直接重放的难度更高。

但须避免以下问题：

- 不要用客户端内的固定“密钥”计算 MAC；它只会被一并提取；
- 不要用可预测时间值作为唯一新鲜度来源；
- 不要让校验失败进入无限循环，便于攻击者识别并补丁；应使用可审计、可区分的安全失败；
- 不要让强完整性破坏正常离线、低端设备或热更新路径；需要灰度和遥测。

## 6. P1：降低静态可还原性，但避免把复杂度当安全性

以下技术可作为“成本提升层”，但不能替代信任边界：

- 每次构建/每个发布渠道使用不同的指令映射、常量布局和元数据编码；
- 让 VM 指令语义、寄存器映射和页布局按构建随机化；
- 把常量索引、寄存器索引和跳转目标与会话/页状态耦合；
- 采用等价但不同的 dispatch 结构，减少单一通用脱壳器的复用价值；
- 对高价值片段实行多版本抽样，而非整包一套恒定逻辑；
- 为构建产物保留内部符号映射、可重复构建材料和回归夹具，避免自身失去可维护性。

避免：

- 仅增加垃圾代码、算术恒等式和更深的平坦化；
- 固定 XOR、固定表替换或静态嵌入种子；
- 检测到异常后 busy loop；
- 将依赖版本、机器指纹或调试环境作为绝对阻断条件；
- 将合法业务密钥或服务端签名能力嵌入 VM。

## 7. P1：反分析与反篡改的工程化设计

### 7.1 检测应成为风险信号，不是唯一裁决

客户端环境存在合法差异。反调试和反篡改应产生多个带置信度的信号，而不是一个信号就无限循环或崩溃。

推荐的信号包括：

- 关键页校验失败；
- 非法状态转移；
- 会话挑战响应不一致；
- 解释器/运行时能力集合异常；
- 高价值动作的节奏、序列和状态不一致；
- 多次不同构建的指纹不匹配。

然后由服务端综合账户历史、设备/会话信誉、交易价值和误报成本进行处置。

### 7.2 不要依赖秘密 API 名称

将 `debug`、`loadstring` 等名字用字符拼装隐藏，只能延缓静态阅读。分析者能从行为、调用结果或局部变量追踪它们。应把这些检查当作廉价辅助项，而不是根信任。

## 8. P2：部署、可观测性与回归验收

### 8.1 发布链

- CI 生成构建唯一的保护材料，并将版本、映射和密钥标识安全归档；
- 保护器输出应有结构化 manifest，供内部验证器使用；
- 绝不把生产解密种子写入日志、崩溃报告或客户端可读配置；
- 采用灰度发布和快速回滚；
- 对不同渠道/版本保留兼容策略，防止完整性策略误伤旧客户端。

### 8.2 持续红队回归

把本次方法固化为自动化验收项：

| 测试 | 通过标准 |
|---|---|
| 静态扫描 | 不出现可直接复原的完整业务源码或固定密钥 |
| 局部插桩 | 无单一稳定点可一次枚举完整程序和全部明文常量 |
| 动态抓取 | 获取单页后不能离线执行任意其他页 |
| 篡改测试 | 修改关键页/映射/状态会被检测并产生可审计信号 |
| 行为回归 | 正常客户端性能、兼容性和热更新不显著退化 |
| 服务端验证 | 重放、越权、状态跳跃和异常频率请求均被拒绝或升级验证 |

## 9. 建议实施顺序

### 第一阶段（优先）

1. 将所有关键业务判定与密钥能力迁移/收紧到服务端；
2. 常量池延迟解码，清除长期明文常量；
3. 将整包原型改为按页/按块解码；
4. 建立对页、状态和关键闭包的多点完整性校验；
5. 把异常信号上传至服务端并接入风险处置。

### 第二阶段

1. 每构建随机化 VM 指令映射和布局；
2. 将页认证绑定到会话状态；
3. 建立保护器 manifest、内部验证器和自动化红队回归；
4. 逐步淘汰无效的反调试 busy loop 与固定编码。

### 第三阶段

1. 对高价值操作引入服务端挑战/能力令牌；
2. 结合遥测做构建差异化和风险分层；
3. 定期重新运行动态脱壳评估，量化“获取完整语义所需时间、探针数量、修改点数量”。

## 10. 验收指标建议

建议不要用“文件有多大、混淆有多深”衡量强度，而采用：

- 获得第一个明文高价值常量所需的修改点数量和执行次数；
- 获得完整可运行原型所需的页数、会话材料和时间；
- 是否存在单一 `qQ` 式全量对象；
- 本地篡改后被服务端发现的覆盖率与误报率；
- 服务端能否独立拒绝客户端伪造的高价值结果；
- 性能、崩溃率和兼容性是否满足发布门槛。

最终原则：保护器应把客户端逆向从“直接读取一个 table”升级为“必须持续控制合法会话、收集多个短生命周期片段并仍无法绕过服务端授权”。
