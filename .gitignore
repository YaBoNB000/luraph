# prebuilt toolchain + interpreter binaries
# (rebuilt in-repo 2026-08-25: see HANDOFF.md §4 rebuild steps)
tools/
.tools/
# build artifacts
luraph-rs/target/
# ...but keep the luau CLI main sources used by the rebuild
!luraph-rs/tools/
