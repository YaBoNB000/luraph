# v15 真多段流水线重写（A2b 立项方案）

> 状态：**立项中（2026-08-28）**。目标 = 把解释器从「单循环寄存器 VM」
> 重写为样本那种「构建/校验/解码/执行分离的 CPS 状态机流水线」。
> 前置：增量 1–7 + A1 已落地（指纹 16/32，执行已内联成样本 fetch 形态）。
> 本文是**分阶段实施 + 每阶段回滚门**的方案；每阶段独立可验证、可回滚。

---

## 0. 为什么单独立项

样本的执行模型与我们**本质不同**：

| 维度 | 我们现状（A1 后） | 样本 |
|---|---|---|
| 执行驱动 | 单 `while true` 分发循环（`run`） | CPS 状态机，handler 返回 `(nextId, 元组)` |
| 指令处理 | 分发树叶子内联执行 | 142 个细粒度 handler，按状态 ID 串接 |
| 帧模型 | 闭包直接调 `run` | 每帧一个新 Lua 闭包（CPS） |
| 流水线 | 解码+执行混在一个 `VM` 闭包 | 构建/校验/解码/执行分阶段独立循环 |
| fetch 循环数 | 2（F11） | 19（F11 黄金值） |

把 F11 从 2 拉到样本量级、并让执行真正变成多段状态机，**只能靠重写执行
模型**——这就是 A2b。它不是「再加几个开关」，是换一台引擎。因此单独立项、
分阶段、每阶段带回滚门。

---

## 1. 保留 / 重写 / 新增

**保留（不动，已验证）**
- `compiler.rs`：AST→字节码、寄存器分配、upvalue 单 cell 模型、SoA 编码、
  `VmProgram`（opmap/slot_perm/carrier/fns/nop_sites）。**编译侧完全不动**。
- `isa.rs`：41 指令、base-94 carrier、varint、Nop 别名。
- 载体密钥流（增量 6）、自修改（增量 3/4）、校验陷阱（增量 2）。

**重写（A2b 主体）**
- `template.rs` 的**解释器发射**：从「单循环 `run`」改为「CPS 状态机
  流水线」。新增 `template_v15.rs`（与现有 `generate` 并存，`generate` 保留
  给非 v15 预设，绝不删改——保住 5.1 矩阵）。

**新增**
- `template_v15.rs`：CPS 流水线解释器发射器。
- 状态元组传参、帧运行器、分段循环的生成逻辑。

---

## 2. 目标架构（映射样本概念 → 我们的实现）

```
return setmetatable({
  -- ① 数字槽：68 原语 + 2 运行器 + 常量        （已具备，增量 2/5）
  -- ② 命名字段：AL/BL/.../RC/pC/XC              （部分具备）
  -- ③ 引导状态机 FC：解码 blob → 建 SoA 原型     （A2b-阶段 B）
  -- ④ 执行状态机 L：CPS 驱动帧执行               （A2b-阶段 C/D）
  -- ⑤ 细粒度 handler 族：每指令 ≥1 个，          （A2b-阶段 D）
  --    返回 (nextId, 元组...)
}, {}):FC()(...)
```

**状态元组**：执行状态打包成位置参数元组 `(pc, V, pf, ups, vargs, lastn,
lastbase, ...)`，在 handler 间以多返回值传递（样本同款）。每构建元组槽序
洗牌（我们已有 shuffle 机制可复用）。

**CPS 循环**：`while flag do ... leaves: 元组 = b:handler(元组); continue ... end`
（我们已有 continue-leaf 骨架，增量 5 已验证可行）。

---

## 3. 分阶段实施（每阶段 = 独立增量 + 回滚门）

> 每阶段的**回滚门**：全量语料 + 多种子 + 矩阵 204 + 预设 405 全绿才提交；
> 任一失败 → 回滚该阶段改动，不带病推进。

### 阶段 B —— 解码流水线独立成段（中风险）✅ 完成（2026-08-28）
把 `parse(FN[i])`（载体→SoA 原型）从 `VM` 闭包里抽出，改为独立的**解码
状态机段**：① 原型级解码循环独立成段（`while di<=#FN`）；② `parse` 本体
改为**显式解码相位状态机**（`st=1..5`：头/upsrc/常量/码流/操作数流五个
相位，状态变量驱动，语义与单体版逐条等价）。产出独立的解码段结构
（CPS 流水线地基）。
- 改动：`template.rs` 解码段 + `parse` 参数化为 `{parse_fn}`（非 v15 保留
  原单体版，字节级零变化，已验证 0 变更）。
- 回滚门：全量语料 + 多种子 5×6 + 矩阵 204 + 预设 405 全绿 ✅。
- 状态：✅ 已完成。解码段与执行段已分离，均为状态机形态。

### 阶段 C —— 执行循环 CPS 化 ✅ 完成（2026-08-28，真 TCO + 内联 Call 系）
把 `run` 的 `while true` 分发循环改为**状态机驱动的 CPS 循环**：
- 每条指令 = 一个 `H[OC.*]` handler，经 `H[oc](a,b,c,d)` 分发；
- `Return` handler 返回信号 `{out, total}`，循环侧尾调用解包返回；
- **Call 系（Call/CallE/CallM/CallT）内联进循环**（不走 `H[]`），
  与真 TCO 配合，深尾递归不叠帧、不溢栈；
- 跳转指令 = 改写 `pc`；`Return` = 经信号返回。
- 回滚门：全量回归 + 针对用例（跳转/闭包/多值/协程/深尾递归）。
- 结论：✅ 完成。30 语料 + 多 seed 5×8 + 矩阵 204/204 + 预设 405/405
  全绿；`tail(5000)` 通过；指纹 F4 90→133。

> **⚠️ 阶段 C 首次尝试结论（2026-08-28，已回滚，务必先读）**
>
> 做了「每指令一个独立 handler（`H[OC.xxx]` 分派）+ `pc` 经闭包传递 +
> `Return` 经信号表返回」的语义保持 CPS 骨架。14/15 个针对语料通过，
> 但 **`functions.lua` 的 `tail(5000)` 深尾递归栈溢出**（丢 `tail-done`）。
>
> **根因（架构属性，非控制流 bug）**：CPS 把每条指令的分发变成一次
> `H[oc](a,b,c,d)` 函数调用。函数调用（`Call` 系 opcode）会递归进
> `run`，于是**每层递归比内联分发多一个 `H[Call]` 栈帧**。`tail(5000)`
> 这类深尾递归（我们的 VM 用 `lastn/lastbase` 模拟尾调用、**不做真
> TCO**，每层本就加一个 `run` 帧）再多一帧，就被顶爆。内联分发版能过
> （少那一帧），CPS 版溢出。
>
> **结论**：CPS handler 分发模型有「每调用 +1 帧」的固有栈代价，深尾
> 递归语料必然触发。要让阶段 C 落地且语料保持全绿，必须先解决栈代价，
> 两条路：
> 1. **内联 Call 系 opcode**（Call/CallE/CallM/CallT 在分发循环里直接
>    处理、不经 `H[]`）——递归不再多那一个帧。中等复杂度，但破坏「全
>    opcode 皆 handler」的纯度。
> 2. **实现真尾调用优化（帧复用）**——尾调用复用当前帧、不再叠加
>    `run` 帧，根治深尾递归栈深。这是「正确」的修法，也是真改进（内联
>    版同样受益），但属调用约定根本改动，对闭包/upvalue/多值风险最高。
>
> 本次已按回滚门**回滚 CPS 改动**，恢复内联分发，全量回归回到
> 204/204 + 405/405 全绿。**阶段 C 暂停，待选定栈代价解法（方案 1 或
> 2）后再推进。**

> **✅ 用户选定方案 ②（真尾调用优化），已落地并推进到阶段 C 完成
> （2026-08-28）：**
>
> **第一步 真 TCO**——不靠识别 VM 闭包/注册表，而是让 `makefn` 闭包对
> `run` 的调用成为**真尾调用**，由 Lua 原生 TCO 复用栈帧：
> - `Return` opcode 由 `return out, total` 改为 `return U(out, 1, total)`
>   （直接返回解包结果，`run` 返回类型随之变为「解包后的多值」）；
> - `makefn` 闭包与顶层调用改 `return run(...)`（尾调用，帧被复用）。
>
> **第二步 重做 CPS 分发（在 TCO 地基上）**——`run` 的分发改为
> 「每指令一个 `H[OC.*]` handler」的 CPS 形态：
> - 非 Call 指令经 `H[oc](a,b,c,d)` 分发；`Return` handler 返回信号
>   `{out, total}`，循环侧 `return U(r[1],1,r[2])`（尾调用）解包返回；
> - **Call 系（Call/CallE/CallM/CallT）内联进 CPS 循环**（不走 `H[]`）：
>   实测真 TCO 只省「闭包→run」层，CPS 经 `H[Call]()` 仍每调用叠一帧，
>   `tail(5000)` 仍溢栈；内联 Call 系后递归不再叠该帧 → **深尾递归不溢栈**。
>
> **验证**：`tail(5000)` 通过；30 语料 + 多 seed 5×8 + 矩阵 204/204 +
> 预设 405/405 全绿；30 个 v15 示例再生成 + 语法全过；非 v15 示例 0 变更。
> 指纹 F4 90→133（H 表 42 个 `=function(`）。
>
> **阶段 C 状态：✅ 执行已 CPS 化（真 TCO + 内联 Call 系），深尾递归不溢栈。**
> 剩余收尾：F11（多段 fetch 循环）/F5（状态返回量级）等指纹随后续结构
> 细化提升；CPS 骨架已立，属「结构同族」的收尾打磨而非新风险。

### 阶段 A —— 操作数散布（✅ 完成，2026-08-28，安全收尾项）
把每个函数的**寄存器/常量/upvalue 散布进随机槽位**（样本 §10.4 / 防御
D1 家族的落地），消灭「操作数是小连续整数」这一静态寄存器恢复锚点：
- **寄存器散布**：逻辑寄存器 `r` → 物理槽 `σ(r)`（每函数独立随机置换，
  ~50% 密度，大帧封顶 +64）。单寄存器操作数直接写 `σ(r)-1`；范围型
  操作（LoadNil/Call 系/Return 的连续区段）保留逻辑基址，运行期经
  每函数**槽表 S**（blob 内，随常量后序列化）翻译。
- **常量散布**：常量放进带填充的更大常量池随机位置（空洞填 nil/
  诱饵数字），LoadK/GetGlobal/SetGlobal 操作数 = 散布位置。
- **upvalue 置换**：upsrc 顺序每函数随机置换，GetUp/SetUp 操作数走
  逆映射；upsrc 里的父帧寄存器引用在 `makefn` 里经**父帧的 S**翻译
  （不需要延迟修补，子函数序列化早于父函数 σ 也不冲突）。
- **溢出表 O**：`nres=255` 调用的返回值可超过寄存器分配数（原始密集
  V 表天然可扩展，散布后固定 S 会越界）——Call/CallE/CallM 的无界写、
  Return 的 lastbase 读、尾部展开值读都带 `S 缺位 → O` 回退。
- 效果：寄存器/常量/upvalue 操作数共享同一不透明数字空间，静态无法
  用「小整数窗口」区分语义；S/κ/τ 全部在载体编码 blob 内，输出不可见。
- 回滚门：30 语料双解释器 + 矩阵 204/204 + 预设 405/405 + 多 seed 5×8
  全绿；`tail(5000)` 通过；非 v15 档 0 变更（legacy parse/模板不动）。
- 新增单测 `scatter_layout_properties`（S 槽唯一/范围/散布 + 线操作数
  越密集区）。

### 阶段 D —— 每帧一个闭包 + 帧运行器（高风险）
把「闭包直接调 `run`」改为「`makefn` 返回的闭包 = 新帧，经帧运行器
（数字槽 `[73]/[18]`）启动」。每帧一个新闭包（样本 CPS 帧模型）。
- 回滚门：闭包/协程/多值全量回归。
- 风险：高。帧边界 + upvalue 传递是雷区。

### 阶段 E —— 指纹收口
F11（多段 fetch 循环）、F12（pcall 帧包装）、F5（方法调用/状态返回量级）
随 B/C/D 自然增长；最后对照样本逐项核对 32 条指纹，补细节（7-bit 分块、
LCG 密钥流等已在增量中具备）。

---

## 4. 风险总览与止损

- **最大风险**：阶段 C 的执行模型改变打爆 upvalue/多值/尾调用。止损：
  阶段 C 单独一个增量，失败即回滚，不带病进 D。
- **性能**：CPS 逐指令经 handler 调用，比单循环慢（样本 ~190 万 op/s
  可作上限参考）。v15 档本就慢于非 VM，可接受。
- **确定性**：所有随机性仍走种子 `rng`；新发射器不引入新随机源即保持
  「同种子同输出」。
- **不碰非 v15 预设**：`generate` 原样保留，5.1 / 默认预设矩阵绝不回归。

---

## 5. 本阶段（阶段 B）落地清单

1. 新建 `template_v15.rs`，先搭出发射骨架（复用 `branch_code`/`bin_op_code`
   /`cmp_code`/`nop_body`/`gen_dispatch_tree`，这些从 `template.rs` 提取为
   共享或复制）。
2. 解码段：把 `parse` 拆成状态步骤，发射成解码 handler 链 + 独立循环。
3. 执行段：本阶段**暂保留** A1 的单循环执行（只把解码独立出去），确保
   阶段 B 风险可控。
4. 回滚门验证 → 提交 → 进入阶段 C。

> 决策点：阶段 B 完成后评估是否继续 C/D（每阶段都可独立叫停）。

---

## 6. 安全加固增量（用户优先级：安全 > 结构，2026-08-28）

> 用户明确：**最重要的是安全性像样本，不只是结构像**。本节记录安全导向
> 增量，按「真实安全差距」排序，独立于上面的结构阶段。

### 安全对账（我们 vs 样本，诚实版）

| 维度 | 我们 | 样本 | 结论 |
|---|---|---|---|
| 字节码虚拟化 | ✅ | ✅ | 持平 |
| 每构建唯一性 | ✅ 种子驱动 | ✅ | 持平 |
| 字节码自修改 | ✅ 增量 3/4 | 不明显 | **我们超** |
| 反篡改（校验和/验证陷阱/金丝雀/时间陷阱） | ✅ | 无（样本弱点） | **我们超** |
| **密钥流来源** | ❌ 曾是明文常量 | 状态机化 LCG | **差距（已修，见下）** |
| 操作数/寄存器不透明度 | ✅ 7-bit varint + 随机槽散布（阶段 A） | 7-bit 分块上下文表 | 持平（样本内联阶梯量级属结构项） |
| 动态分析阻力 | 单循环易 hook | 多段 CPS 状态元组 | ✅ 阶段 C（CPS 执行） |

### S1 密钥流状态机化（✅ 完成，消灭最高危差距）

**问题**：增量 6 的载体 XOR 用 `(K1*i+K2)%256`，K1/K2 是**明文常量**写在
每个分块 handler 里——攻击者找到一个分块 handler 即读出密钥（锁了门把
钥匙贴门上）。

**修复**（样本 `[96]` 同款）：
- 新增 **KSC 槽**（存 LCG 状态，播种）+ **KG 槽**（3 步 LCG 状态机生成器，
  常数内嵌在状态转移体里）。
- 分块 handler **不再含任何密钥常量**，运行时调 `b[KG](b)` 取本分块密钥
  常数，再 `(kv+i)%256` 解；构建期用同一套 3 步 LCG 生成密钥流做 XOR，
  往返一致（30 语料 + 多种子 5×8 验证）。
- 密钥常数只存在于 KG 状态机体内，**解码点静态不可提取**。

**验证**：30 语料 + 多种子 5×8 零失败；矩阵 204/204 + 预设 405/405 全绿；
非 v15 示例 0 变更；`268435456` 仅出现于 KG 生成器（1 处）。

**后续安全项**（按差距排序）：~~操作数不透明化（S2）~~ ✅ 阶段 A 完成
（操作数随机槽散布 + 7-bit varint）；~~执行段 CPS 化~~ ✅ 阶段 C 完成。
剩余为结构同族收尾（阶段 D/E：每帧闭包、指纹量级打磨）。
