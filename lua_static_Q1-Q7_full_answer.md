我刚核对过：7 个脚本确实合计 **502 行**。先划清两点：

- **没有按机制单独计时。**下面的成本排序来自实际阅读、写工具和返工记录，不是实测工时比例。
- 你现在提供了 Luraph 背景，但我上次没有识别出产品归属，也不知道生成器内部的 KT/HBOOT 命名。尤其 **②“异构编解码器”不能直接当作我已经验证过的事实**。

## Q1｜成本排序

### 综合工作量：③ ≳ ⑦ > ① > ⑤ > ⑥ > ④；②需要单独说明

|排序|机制|本次实际消耗|
|---|---|---|
|1|**③ 引导碎片／解析器入密**|需要先取得加载器，再解释 `Cl` 的三元组、区分 handler 与 `w=200` 的解析器，之后才能知道载荷格式。成本在**跨层依赖和格式重建**，不在模运算难算。`decode_segments.py` 61 行、`parse_payload.py` 111 行承担了主要工作，但不能把这 172 行全部归功于“加密”。|
|2|**⑦ 其他：作用域绑定、格式假设、VM 寻址核对**|这是最容易造成错误结论的部分：同名局部变量取错两次，稠密寄存器假设错一次。拿到明文处理器后，还要确认 `a=SD,b=SC,c=SA,d=SB`，以及物理槽和 `S` 映射槽的区别。|
|3|**① KT 查表钥匙装配**|按 `y/Y/N/I` 常量表和算术索引来理解。需要写白名单折叠器、绑定作用域、确定数组来源。**第一次有工具建设成本，之后重复成本很低**；大整数相消成小索引本身没有形成难题。|
|4|**⑤ 脚手架双表碎片＋首调装配**|按 `b[70]/b[48]`、`b[23]` 首次加入后置 nil、`b[63]` 保存状态来理解。关键是确认五次调用共享状态，不能各段重置。理顺之后就是机械的数据变换，没有发生这部分的算法返工。|
|5|**⑥ 不透明谓词／状态机**|外观很大，但 94 个业务为空的处理器具有同形结构，下一状态还是明文常量。数学谓词没有难住我；返工是正则没有兼容 `then` 前的空白。完整 CFG 更多是后续确认和交付证据，不是核心突破必须逐节点苦读。|
|6|**④ 两段式自引用掩码**|理解“哈希的是前 64 字节密文、不是明文”以后很便宜。整个密文已在文件里，所谓自引用没有引入未知信息。没有在这一机制上返工。|

### ②为什么不能直接排？

实际恢复出的路径是：

- 30 张数组拼成 2,686 字节元加载器；
- 该加载器对 44 个片段使用同一套 **Base94 解载体＋模递推减字节**；
- 片段之间变化的是编号、种子、槽位和长度。

**我没有为这 44 个片段分别识别出不同算法，也没有编写“按片段选择异构 codec”的工具。**

如果你说的 HBOOT 就是那段 2,686 字节元加载器，那么它的提取成本可放在③与①附近。但我不能把这个成本归因于“异构性”：从这次攻击证据看，那一点没有得到独立确认。

### 单按返工次数，排序反而更明确

**⑦：3 次主要修正；⑥：1 次；其余没有独立记录的算法返工。**

不要把“代码看起来最复杂”和“让我出错最多”混在一起。也不要把 502 行全算成防御收益：其中 47 行主要做语义标注和产物整理，64 行主要做反向校验。

---

## Q2｜探针不受可见分支约束，我怎么处理？

**先标记为符号未知继续分析，不先造完整运行时，更不会随便选一个地址代入。**

处理顺序是：

### 1. 先查它是否真的影响所需结果

本次 `li` 就是例子：

```lua
pi[(opcode + li) % 256] = handler
pi[(instruction + li) % 256](...)
```

同一个未知量出现在注册和查询两边，可以消去。它未知，但不妨碍确定业务语义。

### 2. 查有效位数，而不是看名义上的地址位数

这是对你这个反事实最重要的一点：

**如果仍然沿用本次的整数线性递推、模 `2^28`、最终只取低字节，那么未知种子对字节流的影响只需要看低 8 位。**

因为：

```text
((a*x+c) mod 2^28) mod 256
= (a*(x mod 256)+c) mod 256
```

在这些运算保持精确整数语义的前提下，可能只剩 **最多 256 类字节流**，不是需要恢复完整地址。换成使用高位、非线性状态或影响控制流的算法，这个简化才可能失效。

### 3. 查隐式约束

“没有 `if` 检查”不等于完全没有约束：

- 后续是否必须成功解析？
- 长度、索引、校验值是否有要求？
- 解码结果是否被当作特定格式读取？

代码强制的成功条件可以作为路径约束；“看起来像 Lua”只能作为候选筛选，不能冒充证明。

### 4. 真正不可消去时，保留条件结果

例如报告：

```text
解码结果 = D(address_hash)
已恢复不依赖 address_hash 的部分；
其余存在多个静态可行结果，不能唯一确定。
```

我通常不会为了恢复一次分配地址，去猜完整的分配器、GC、堆历史和 ASLR 状态。那往往是高成本且不可靠的方向。

**若关键阶段确实需要文件里不存在的信息，我会停在那个边界，而不是把猜测写成还原。**本次 `89621` 的推导严格限于“能通过该检查的路径”，不是断言所有实际运行都一定通过。

---

## Q3｜换成小号自定义字节码，白名单折叠器还能覆盖吗？

**现有折叠器不能直接覆盖。**

它只认识表达式：常量、数组索引、加减乘除、取模。它不认识：

- PC 和指令边界；
- 寄存器状态；
- 分支和循环；
- 间接访存；
- 自修改指令；
- 构建相关的指令编码。

我需要增加的主要工具是：

1. **指令格式和操作码语义提取。**先读可见分派器，不会一开始就造完整 Luau 编译器。
2. **小 VM 反汇编器／语义提升器。**把每条指令转成明确的寄存器、内存和控制流操作。
3. **常量传播、控制流分析和循环摘要。**
4. 必要时增加位向量约束求解，正确处理位宽、截断和溢出。

原来的算术折叠器仍可作为其中一个零件，但不再是整套方案。

### 工作量估计——这是反事实估计，不是实测

以下以本次“恢复与验证的分析工作”为 1，不含报告排版：

|小 VM 的性质|首次适配的总工作量粗估|
|---|---|
|输入全为常量、指令格式清楚、主要是直线算术或简单有界循环|约 **1.5～3 倍**|
|有间接访存、复杂别名、数据相关分支、变化的指令边界或自修改|可能 **3～6 倍以上**|
|关键状态依赖不可获得的运行时输入|不只是翻倍问题，可能无法唯一静态确定|

**“30 行分派器”不能给出复杂度上限；“每构建随机 opcode 编号”也不能自动推出很高成本。**如果只是编号置换，识别一次语义以后，映射可以参数化。

还要划清纪律：写一个 Python 小解释器，沿 PC 把这段字节码跑完，虽然没启动 Lua，仍然是在仿真指令流。我不会把它偷换名称，说成与本次完全相同的纯静态方法。原任务约束不变时，优先做语义提升和静态化简；是否允许指令仿真需要另行确认。

---

## Q4｜地址在 N 个预烘焙变体中选一个呢？

**我不会先恢复地址，也不会机械地完整逆向 N 次。**

顺序大致是：

1. **缩小可达候选集合。**选择表达式是否真能选到全部 N 个？有哪些可以证明的范围、位约束？
2. **找共同接口和汇合点。**这些变体是否最后都交付相同格式的解析器、常量表或字节码？
3. **选依赖最少的一个先分析。**
4. **对其他变体做等价性核对或聚类。**如果只是变量重命名、常量拆分、opcode 置换，归一化通常比重新理解便宜。

但有一个不能省略的限制：

> 解出一个变体，只能证明“这个变体产生什么”，不能自动证明实际运行选中的就是它，也不能证明其他变体相同。

如果能证明所有可达变体交付相同业务结果，就无需知道地址。如果确实存在不同语义而无法判断选择，我会给出条件结果或结果集合。

成本取决于**不同语义的数量**，不只是 N：

- 大量同模板变体：分析一个，加上其余的差异／等价性核对。
- 真正独立的 N 套实现：可能接近逐套分析的成本，静态等价性证明也不保证容易。

另外，地址即便有很多位，最后只是选 N 项，对变体选择的影响也至多是那 N 种可能，不能直接按完整地址熵计算保护强度。

---

## Q5｜换个新种子，复用率多高？

**这 502 行是针对本样本的工具，不是已经验证过的通用解混淆器。**

我没有第二个构建做迁移测试，不能报“实测复用率”。按功能块粗估：

|变化范围|估计|
|---|---|
|仅换加密常数，名字、槽位、布局、标签和指令语义映射均保持不变|大部分可复用，约 **80%～90%**，仍需重新验证|
|正常构建级重随机化：名字、槽位、片段长度、标签、opcode 映射等一起变化|直接可拿来用的通用组件约 **25%～40%**；算法和分析思路约 **70%～85%** 可迁移|
|连 codec 或元 VM 语义也改变|上述比例不适用，需要重新评估|

这里的百分比是工程估计，不是逐行 diff 结果。**面对正常重随机化的新构建，我预期当前管线不能原样端到端跑通。**

可以直接复用的核心：

- 字符串词法遮罩；
- 白名单算术求值；
- 参数化的 Base94、小端、变长整数读写；
- 字节流变换和反向编码验证框架；
- “从消费者反推格式”的分析方法。

必须重新绑定或推导的具体地方很多：

- `static_extract.py` 写死了 `y/Y/N/I`、`ma/kz` 等名字；
- 外层 `b[43]` 等槽位以及双表装配位置是本样本特定的；
- 五段长度 `39,39,39,39,38` 被直接写进工具；
- `outer_cfg.py` 写死了函数签名、部分处理器名和返回状态；
- `parse_payload.py` 写死了节标签，并按单原型处理；
- 第六条 opcode 补丁的位置是固定的；
- `make_semantics.py` 的 opcode 名称列表和寄存器解释是人工确定的。

**最后一项尤其危险：新构建可能还能吐出 CSV，但语义标签已经错了。**“工具没有报错”绝不等于复用成功。

---

## Q6｜有没有被骗过？

**有，但主要是抽取和语义假设上的误导，不是长时间追着诱饵载荷跑。**

### 最接近“把错误来源当权威”的两次

1. 找字母表种子 `u` 时，取到了外层的 `local u=0`，而不是 UB 内部的那个。结果 94 个字符不再互异，断言失败。
2. 找加载器状态 `_t` 时，取到了外层那个 `C[8]~=nil and ...` 的无效局部量。白名单解析器拒绝了表达式。

**这两次说明：我当时的薄弱点是名字匹配不等于作用域绑定。**

### 最容易让正确结果被误判为错误的一次

我假设 `S` 是 `1..nregs` 的稠密排列，但实际是：

```text
nregs = 3
S = {8,9,4}
```

差一点把正确解码当成坏数据。回读普通指令与 CALL 的消费者后，才确认是稀疏物理槽映射。

### 还有一次较小的诱饵命中

除了报告记下的四次主要脚本修正，生成定位信息时，简单的原文查找也出过错：

```text
.find("ny=") → 5679
真正的 ny 字段 → 26669
```

前者落在 `mp` 的随机长字符串内。之后改为遮罩字符串再找字段。

这是确实被随机内容干扰过一次，但影响的是证据偏移定位，不是业务控制流结论。

### 没发生的事

- 没有把 `mp` 长串当业务字节码深入解码；
- 没有把返回后的第六条指令当活路径；
- 没有把 `b[694]/b[695]` 副本当作真正密钥函数；
- 没有把外层 `gU` 的明文映射当唯一权威，内部实际消费者的映射是独立解出的。

**差点骗到我的不是谓词的数学，而是“看起来合理”的通用结构假设。**

---

## Q7｜什么会让我停止纯静态？

### 在这次明确禁止执行的约束下：不会因成本高就自行改做动态

代价再高，也应当交付：

- 已确定部分；
- 条件结果；
- 未知量及其依赖；
- 为什么无法继续。

不能偷偷运行以后继续声称纯静态。

### 如果下一阶段允许动态，我会在这些情况下申请切换

1. **出现不可消去、无法获得的运行时输入。**  
   尤其是关键解码值不在文件中，无法约束，而且有效未知空间确实很大。

2. **为了继续必须猜宿主的具体状态。**  
   例如要还原分配器／GC 历史才能确定某个地址。我通常不会先把完整宿主模型造出来。

3. **路径或别名状态持续膨胀，静态分析没有缩小未知集合。**  
   间接跳转、可变指令边界、自修改等机制如果造成这种效果，观察一次中间结果可能更经济。

4. **目标只是一个具体执行实例，而不是证明所有环境下的行为。**  
   此时动态观测的收益可能明显高于继续做全路径静态证明。

不过动态也不是万能替代：**跑一次只能看到那一次路径，不能自动证明 N 个变体等价，也不能凭空取得尚未提供的外部秘密。**

我没有根据这一个样本建立“超过固定 X 倍就切动态”的实测阈值。真正的判断是：继续静态是否还在减少不确定性，以及目标是否确实需要全环境结论。

---

**直说这次防御的效果：它显著增加了定位、绑定和格式重建工作，但没有给本样本引入一个无法从文件确定的必要秘密。**

这次成本比较高的部分，是**延迟看见下一层结构，以及让我容易绑定错对象**；不是大数表本身，不是 105 个路由节点的数量，也不是两段式掩码的“自引用”这个名字。
