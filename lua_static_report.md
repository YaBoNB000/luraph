# Lua / Luau 混淆样本纯静态还原报告

## 结论先行

**业务载荷的等价逻辑是：**

```lua
print(12)
```

更精确地说：在保护层的环境检查通过后，从 `getfenv(0)` 取得 `print`，以数字 `12` 为唯一实参调用一次；处理一个随后不再使用的返回值槽，最后不返回任何值。

这不是“找到了字符串就猜是 print”：已经恢复出 **1 个函数原型、6 条指令、6 个常量槽和全部 43 个指令处理器**。第 1～5 条指令形成完整、无分支的可达路径，第 6 条位于 `RETURN` 之后。普通环境中的可观察业务效果是输出 `12`。

**这不是原始源码的逐字恢复。** 原来的变量名、注释，以及 `12` 是否由编译前的表达式折叠而来，无法唯一确定。保护层也不能简单删去后声称与整个样本完全等价：它会进行多项环境探测，失败时可能永久循环。

### 分析边界与样本身份

- 输入：`uploads/test.txt`，119,396 字节，ASCII，主体为一个长行。
- SHA-256：`a531573ee220d065de401cd83e8be75b146d8344745be65aec633af10af8b6e4`。
- **没有运行上传的 Lua、任何解码后的 Lua、`loadstring`，或 VM 指令。**
- 使用了自己编写的 Python 辅助程序，进行词法遮罩、受限算术常量折叠、字节变换、二进制格式读取、CFG 遍历和反向编码校验。
- 算术折叠器只接受数字、已提取常量数组的索引和白名单算术运算；不执行上传文件中的函数，也不把解出的源代码交给解释器。
- 下文的“解密／解码”指这些静态数据变换；“预期输出”不是实际运行日志。没有联网查询混淆器签名。
- 字节偏移均从原始文件的 **0** 开始计算；核心定位信息另存 `recovered/source_anchors.json`。

---

## 1. 攻击目标：最终试图还原什么

本次目标分为五项：

1. 从外层状态机找出真正被拼接、传递并解释的载荷，而不是把大字符串一律当作字节码。
2. 恢复载荷、常量表、函数元信息及真实可达控制流。
3. 恢复 VM 的操作数布局、寄存器寻址和指令语义。
4. 恢复被二次编码的加载器与指令处理器文本，但不执行它们。
5. 区分反调试／反 Hook／完整性检查、反静态分析噪声，以及纯兼容性探测。

**完成程度：**业务逻辑与本样本指令全部还原；43 个处理器文本全部提取；主要保护路径全部解释。没有认定混淆器品牌，也没有声称恢复原始源码排版、局部变量名或通用 VM 的全部语言兼容性。

---

## 2. 逐步流程：假设 → 操作 → 观察 → 结论

以下按数据依赖关系整理为 **14 个主要步骤**，不是声称只进行了 14 次阅读或工具调用。

### 步骤 1：把字符串与代码结构分离

- **假设：**长行中的随机字符会制造大量假的关键字、赋值和函数名匹配。
- **操作：**词法识别短字符串及 `[[...]]`、`[=[...]=]` 长字符串；暂时用编号遮罩，再按分号分行阅读。
- **观察：**共 91 个字符串字面量。代码使用 `+=`、`continue`、`local x = if ... then ... else ...`，并引用 `buffer`、`vector`、`bit32`。
- **结论：**这是明显的 **Luau 方言**，不能按标准 Lua 5.1 直接理解语法；词法遮罩也是防止误定位的必要步骤。

### 步骤 2：从最终调用点做反向切片

- **假设：**真正入口在最后的调用链，不一定是最大的字符串。
- **操作：**追踪 `setmetatable({...},{}):vH()(...)`，以及 `iH` 中的闭包和 `C` 的写入。
- **观察：**`C[7]` 包装调用 `C[6](C[1]..C[2]..C[3]..C[4]..C[5])`；`[92]` 只安装 `UB` 到 `C[6]`；`vH` 最终返回 `C[7]`，文件末尾才调用它。
- **结论：**真正的业务载体是 **5 段字符串的拼接结果**，不是 `mp` 大字符串。调用方传入的 `...` 没有作为业务参数传进 `UB`。

### 步骤 3：还原外层路由，不跟随无效条件

- **假设：**`My` 是扁平化状态机，返回元组的首项是下一状态。
- **操作：**对小整数状态证明不透明谓词；解析阈值树并提取静态 CFG 边；只读取处理器的常量返回值，不调用处理器。
- **观察：**104 个不透明分支包装可消去；105 个路由叶子均位于正常链上，其中 94 个仅计算未使用的局部量并轮换临时参数。
- **结论：**有效前半段是 `3:cB → 4:KM → 7:Ra → 8:Mu → 10:uc → 12:YO`；随后经过 94 个空业务处理器，到 `217:[92] → 218:iH → 231:Rx → 233:jN → 236:dl`。得到五次字符串解码的确定顺序。

### 步骤 4：识别 32 位字表及小端拆字节

- **假设：**`b[67]` 的 50 个数字编码了五块各 40 字节的数据。
- **操作：**合并分裂的算术常量；将减法与加补数两种写法统一；按源码的 `%256`／右移顺序取字节。
- **观察：**每组恰取 10 个 32 位字。第一层掩码为 `(3448791227 + index*3668954807) mod 2^32`。输出截取长度为 `39,39,39,39,38`。
- **结论：**字表是小端字节容器；这里仅去掉第一层掩码，还不是最终明文。

### 步骤 5：恢复有状态的五次密钥生成

- **假设：**`b[5]` 的状态更新决定第二层 XOR，不能每段重置。
- **操作：**静态折叠初次 `b[23]` 加入后置空的分支；按步骤 3 的顺序计算五次三段式模递推，再做逐字节 XOR。
- **观察：**五个密钥依次是 `262786479, 26315172, 117049539, 224507872, 98978423`；拼接得到 194 字符文本，字节和为 `16047`。
- **结论：**与 `cB` 计算的 `C[8]=4603+11444=16047` 一致。外层恢复不是靠“看起来像文本”判断，而有样本自身校验支持。

### 步骤 6：还原 VM 解码前置常量

- **假设：**`ma`、`kz` 分别与载体字母表和转义有关。
- **操作：**折叠 `y/Y/N/I` 的大数索引；读取 `A[(ma[i]-state%256)%256]=i-1` 和每组连续五次乘 `94` 的消费者；读取 `kz` 的 `50+10` 分组。
- **观察：**得到 94 个互异字符的有序字母表，以及 10 条“5 字符标记 → 1 字符”的映射；标记以 ASCII 89，即 `Y`，开头。
- **结论：**这是自定义字母表的 **Base94 类载体**，不是 Base64。外层五段必须先拼接再消转义，因为标记横跨了分段边界。

### 步骤 7：用成功分支约束消去环境密钥

- **假设：**`debug.info(loadstring,"s")` 的结果既作门禁，也混进了后续解码种子。
- **操作：**还原乱序字符表中的 `loadstring/debug/info/s/[C]`；只分析不会进入永久循环的分支。
- **观察：**正常继续的必要条件是结果严格等于 `[C]`；其 31 倍滚动哈希恰为 `89621`，使种子中的 `(mD-89621)*...` 项为零。
- **结论：**不必查询真实运行环境，也不必伪造／绕过检查；根据成功路径的常量约束即可确定静态解码密钥。

### 步骤 8：解出第一段加载器文本

- **假设：**连续的 30 张小字节数组构成被分割的源码，而非最终 VM 指令。
- **操作：**严格按 `kO[#kO+1]=...` 的源码顺序拼接，使用已折叠的流递推解码。
- **观察：**29 张数组各 90 字节，最后一张 76 字节，共 2,686 字节，解出可读文本 `return function(HQ, hqi, AL, BYTE, CHAR, FLR, SUB, LS, KA, KB, KC, KM) ...`。
- **结论：**它是下一层片段加载器；源码中原本会发生的 `loadstring(...)()` 被静态文本提取替代。

### 步骤 9：根据三元组恢复全部处理器和解析器

- **假设：**`Cl` 的重复三元组是路由描述，不是普通数据表。
- **操作：**读取加载器的 `w=hqi[hi]`、`seg=HQ[hqi[hi+1]]`、`flen=hqi[hi+2]`；依次解码相应长字符串。
- **观察：**44 个三元组：43 个对应操作码 `0..42`，一个 `w=200` 对应 9,848 字节的字节码解析器；43 个处理器合计 22,623 字节。
- **结论：**完整恢复了处理器文本和载荷格式定义。没有把 `200` 误当成运行时 VM 操作码。

### 步骤 10：消去地址相关路由盐并确认操作码映射

- **假设：**`tostring({})` 产生的地址相关字符串可能妨碍静态确定分派。
- **操作：**比较注册表达式 `pi[(na+li)%256]=handler` 与调用表达式 `pi[(_d+li)%256]`；另外恢复 `j` 表。
- **观察：**同一个 `li` 同时加在注册键和查询键上，模 256 加法在操作码域内是双射；`j` 是 `0..41` 的一张排列，另有补丁码 `42`。
- **结论：**不知道地址也能消去这个盐。实际 handler ID 由解出的操作码直接决定。

### 步骤 11：解出 113 字节载荷并读取分节格式

- **假设：**194 字符外层明文仍包含载体编码和另一层字节掩码。
- **操作：**消转义，按五字符解四字节，读取 4 字节长度前缀；根据解析器的“前 64 字节 + 密文哈希重置”规则独立解码数据。
- **观察：**194 字符 → 150 字符 → 120 字节容器 → 113 字节有效载荷。分节标签依次为 `237,118,221,82,86,191`，恰好读完所有字节。
- **结论：**恢复一个函数原型；字节码不是原生 `luac` 格式，而是自定义分节格式。

### 步骤 12：恢复常量池与操作数列，并校验

- **假设：**`C` 区还有独立加密，`SA/SB/SC/SD` 是按列存放的操作数。
- **操作：**以常量种子 `22229` 解出 43 字节常量池；按类型标签及数值的尾数／指数编码解析；对四列求和。
- **观察：**常量槽为 `659975,12,"print",nil,nil,581310`；四列合计 `411152`，与解析器 `OS[1]` 完全一致；常量池也恰好消费 43 字节。
- **结论：**常量表与指令字段有边界和校验的双重支持。`S={8,9,4}` 是稀疏物理槽映射，不是 `1..nregs` 的稠密排列。

### 步骤 13：从取指顺序还原业务控制流

- **假设：**应按 VM 实际传参排列解释操作数，而不是按字段名字猜 A/B/C/D。
- **操作：**对齐 `VK=SD, Fy=SC, Hy=SA, rm=SB`，再匹配处理器文本，做静态数据流替换与可达性分析。
- **观察：**指令为 `GETGLOBAL → LOADK → CALL → MOVE → RETURN`；全局名是 `print`、参数是 `12`；尾部 NOP 不可达。
- **结论：**业务等价于 `print(12)`。`MOVE` 所保存的返回值未被使用；不能据此唯一推断原源码是否写了某个局部变量。

### 步骤 14：审计保护逻辑并做反向编码验证

- **假设：**可读结果仍需要独立一致性检查，并须区分真实保护与假循环。
- **操作：**审计入口、`Rx/jN`、内部加载器前置检查；将恢复的文本／字节按相反变换重新编码，比对原密文。
- **观察：**44/44 片段的有效长度部分、113 字节载荷、43 字节常量区、194 字符外层载体均逐字节往返一致。空表 `zc` 前的 `JT` 折叠为 `0`，其危险循环不可进入。
- **结论：**完成静态证据闭环。所述输出并非动态观察，保护逻辑的实际触发情况仍取决于宿主。

---

## 3. 依赖的特征：真正用来突破的具体签名／模式

这里列的是**本次实际用到的代码特征**，而不是泛泛的“熵很高”“像某某混淆器”。定位以原字段名、精确表达式和必要的字节偏移为准。

### A. 入口、状态机和数据流

|编号|抓住的具体模式|它解决了什么|
|---|---|---|
|F01|`+=`、`continue`、`local h=if ... then ... else ...`|确认 Luau 方言，避免拿标准 Lua 语法解释样本。`buffer/vector` 只是进一步印证，不足以单独断言宿主一定是 Roblox。|
|F02|末尾 `setmetatable({...},{}):vH()(...)`|定位真正入口及两次调用。元表是空表，不能凭 `setmetatable` 就猜还有隐藏 `__index` 分派。|
|F03|大表同时有 `[76]=string.byte`、`[107]=string.char`、`[59]=function(b,E,...)return E(...)end` 等|恢复数值槽的别名意义，识别 `b[59]` 只是调用桥，而不是解密器。|
|F04|`J={[4]="tB",[7]="tB"}` 与 `b[J[4]](...)`；`tB` 的 `K[7]="My"`|直接找出路由目标藏在哪个表槽。`tB` 的两支最终都调用 `My`，不能把阈值 122 当成两套不同解释器。|
|F05|处理器形如 `return 58,C,FO,kD,UG,Ox,PR`；调用点 `ya,_Z,...=b:GO(...)`|由返回元组的**第一个槽**认出状态值。临时参数置换不是业务寄存器语义。|
|F06|`(ya*ya+ya)%2==0`、平方非负、`(ya+1)^2>=ya^2`|在已证明的非负小整数状态域中消去不透明谓词，恢复阈值树。最后一个条件不是对所有实数恒真，分析没有脱离取值域滥用。|
|F07|`local x=C[8]~=nil and A or B; return 固定状态,C,...`|`x` 从未被用来分派。据此识别 94 个“看似检查状态、其实业务为空”的处理器。|
|F08|`iH` 的 `C[6](C[1]..C[2]..C[3]..C[4]..C[5])`，偏移 3368|最重要的反向数据流锚点：确定真正载荷及五段顺序，排除只因大就关注 `mp` 的误区。|

### B. 外层字表与载体编码

|编号|抓住的具体模式|它解决了什么|
|---|---|---|
|F09|`b[67]` 恰有 50 个 32 位量；五个消费者分别取 `1..10`、`11..20` 等|把字表分成五块，而不是当作操作码序列或字母表。|
|F10|连续四次 `%256` 后 `/256`，或 `band(x,255)` 后 `rshift(x,8)`|确定小端拆字节顺序；两种风格是同一种算法。`word-mask` 与 `word+(2^32-mask)` 在模域中等价。|
|F11|`b[43]+b[98]`、`b[30]-b[25]`，以及 `b[70][i]+b[48][j]`|恢复被分裂的常量。48 项大表里既有真参数也有噪声，不能整表扔掉。|
|F12|`if b[23]~=nil then ... b[23]=nil end`，以及持久化 `b[63]=z`|确认只有首次加种子偏移，五次解码共享 PRNG 状态。每段独立重置会得到错误结果。|
|F13|分段末尾／开头分别出现 `YQ` / `HFh`，以及 `Y62` / `CR`|证明五字符转义标记可以跨段；必须先拼接再消转义，不能逐段 Base94 解码。|
|F14|`Rx` 对五段字符串逐字节累加，`jN` 比较 `C[9]==C[8]`|提供明文正确性的内部证据：字节和 `16047`。这是弱完整性检查，不是加密哈希。|
|F15|`ma` 有 94 项；`A[(ma[i]-u%256)%256]=i-1`；消费者五次 `v=v*94+A[BYTE(...)]`|由**生成者和消费者同时匹配**确认自定义 94 进制字母表；不是只扫到数字表就猜编码。|
|F16|`kz` 有 60 项；每 5 字节构造 key，再取第 `50+i` 字节作 value；`BYTE(q,k)==89` 时跳 5|识别 10 个以 `Y` 开头的替代标记。外层 `gU` 的明文表与独立解出的映射相同，可交叉核对，但真正执行路径使用内部解出的 `z`。|
|F17|五字符累计后四次 `%256`，再读首四字节长度并 `SUB(E,5,4+BK)`|确认载体为五字符／四字节加 32 位小端长度头，排除将尾部填充误当业务字节。|

### C. 内层源代码加载与分派

|编号|抓住的具体模式|它解决了什么|
|---|---|---|
|F18|`y/Y/N/I` 四张 64 项表；索引如 `880034467737+512053045486-1392087513201`|该例折叠为 22，故是常量索引混淆，不是超大寻址。由此恢复各层实际 PRNG 参数。|
|F19|乱序 `表[位置]=ASCII` 加独立顺序表，最后 `..CHAR(表[顺序[i]])`|还原 `debug`、`info`、`loadstring`、`load`、`[C]`、`s`、`function` 等关键字符串，定位反 Hook 门禁。|
|F20|`if not EP then while true do end end`，且 `EP` 只在 `debug.info(loadstring,"s")=="[C]"` 时成立|成功路径固定 `mD=hash31("[C]")=89621`，使解码种子的环境项归零。这是基于路径约束的常量推导。|
|F21|`_g,MH,c,...,Rs` 连续 30 张数组，加大量 `kO[#kO+1]=数组[i]`|由引用顺序确定拼接次序及 2,686 字节源码载体；不是按变量名或文件邻接位置猜顺序。|
|F22|解出的加载器出现 `w=hqi[hi]`、`HQ[hqi[hi+1]]`、`flen=hqi[hi+2]`、`hi=hi+3`|正式确认 `Cl` 每个三元组是 **处理器编号／长字符串槽／有效文本长度**。|
|F23|加载器中 `if w==200 then BSS=... else HW[w]=LS(...)() end`|区分字节码解析器与 43 个指令处理器；解释为什么最大长字符串槽 805 不是业务程序，而是解析器源码。|
|F24|所有解码片段开头的 `return function(E,a,b,c,d)`，随后同一组 `E[1]..E[22]` 解包|恢复统一 handler ABI；重复模板本身暴露了寄存器、常量、环境、upvalue、调用捕获等槽位。|
|F25|`pi[(na+li)%256]=Ju` 与 `pi[(_d+li)%256](...)` 使用同一 `li`|消去 `tostring({})` 的地址相关盐，不需要获知宿主地址或运行 `tostring`。|
|F26|`j[v]=(编码值-(基数+v*步长)%65536)%256`|恢复 42 个操作码的排列；不把 `j` 的索引和最终 handler ID 混为一谈。|

### D. 字节码、常量与控制流

|编号|抓住的具体模式|它解决了什么|
|---|---|---|
|F27|`local _d=Xl[NC]; if _d then ... NC+=1 ... pi[...]`，偏移附近 110608|明确找到真实取指—递增 PC—分派循环，而不是把外层 `while` 或空表假循环当 VM。|
|F28|`gy.C,gy.SB,gy.SC,gy.S,gy.W,gy.SA,gy.SD` 的别名赋值及 `VK=nq[NC]` 等|确认按列存储的 SoA 格式，实际 handler 参数是 `a=SD,b=SC,c=SA,d=SB`。字段名不能直接当标准指令参数顺序。|
|F29|普通运算用 `V[a+1]`，CALL/RETURN 却用 `V[S[a+i]]`|发现“物理槽”和“逻辑顺序”两种寻址；本例 `S={8,9,4}`。这是正确读出 `print(12)` 的必要条件。|
|F30|GETGLOBAL/LOADK/MOVE 的主体未引用 `c,d`，但这些位置有大数|确认大操作数是未用字段填充，不是必须解释的寄存器编号；同时它们仍参与整体操作数校验。|
|F31|解析器先解前 `min(n,64)` 字节，再对**原密文**前 64 字节做 `hf=hf*31+BYTE(s,i)` 并重置种子|恢复分段流掩码，避免错误地连续解到底，或用明文计算哈希。|
|F32|标签 `118/221/237/86/82/230` 与 `got>=6`；未知标签落入代码节|恢复六类必需分节及可跳过的诱饵节。样本实际代码标签为 191；没有无依据地固定所有未知标签都必须等于 191。|
|F33|参数叫 `r16`，实际传入的是外层 `l`；主体判断字节 `<128`，按 `128/16384/2097152` 组合|识破误导性名字：它是最多四字节的变长整数读取器，不是固定 16 位读取。第四字节作为完整末字节使用，不完全等同于任意长度标准 ULEB128。|
|F34|常量标签 `0/1/3/else`；字符串两字节长度；数字连续两段变长整数|识别 nil／布尔／字符串／数字，而不是找 IEEE-754 双精度字节。数字由 `m * 2^(floor(kp/2)-2048)` 和奇偶符号位组成。|
|F35|`p~=cstart+csl` 与 `PF_[di].ck~=OS[di]` 都导向死循环|提供常量区长度与四列操作数和两项强定位证据；同时明确该 checksum 不覆盖 opcode 列。|
|F36|处理器 24 的 `(63371*a+41221)%65536` 及 `q1/q2/q3` 拆五个字节|发现另一种内联短字符串指令；本载荷未用它，因此没有把 `print` 错归因于这种编码。|
|F37|GETGLOBAL 的 `G[C[b+1]]`，且 `G` 槽对应 `getfenv(0)`|从实际数据流证明 `