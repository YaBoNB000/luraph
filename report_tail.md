print` 是业务调用目标；它不是仅出现在某个诱饵字符串中。|
|F38|处理器返回 `{j=b}`；VM 令 `NC=hk.j`；其他非空返回解释成 `{values,count}`|恢复绝对跳转及返回协议。本例第 5 条直接返回，故第 6 条不可达。|
|F39|`NQ=W[1].W; NQ[6]=Y_`，其中 `Y_=42`；原第六码为 14|识别执行前 opcode 补丁；查验两处理器后确认均 NOP。不能将补丁误解为关键业务解密。|
|F40|`local zc={}` 后看似取指的循环，但 `JT=(... )%7=0`|确定是假解释器形状，不会取到 nil 再做比较；不是漏解的第二个业务 VM。|

### E. 保护与反静态噪声

|编号|抓住的具体模式|它解决了什么|
|---|---|---|
|F41|`debug.info(f,"l"/"s")` 加 `xpcall` 错误字符串中的 `:(%d+):`|识别“源码行号与报错行号一致性”检查，以及原生函数来源 `[C]` 检查，而不是一般错误处理。|
|F42|`newproxy(true)`、保护元表、`__tostring/__concat/__call/__iter` 指向同一陷阱函数|识别可能在转字符串、拼接、调用、迭代时触发的诱饵；没有声称“仅附加调试器就一定触发”。|
|F43|大量 `local x=A+B; local y=x*C-D; if y*y>=0 then local z=y-x end; local w=y+1-1`|纯局部、无消费者的算术填充；能切掉，但不能据此把邻近真常量也一起删除。|
|F44|`mp` 的 10,500 字符长串无进入有效载荷切片的引用；`DX/ny`、`b[8]/b[58]` 等不在正常调用图上|识别大块诱饵与未用 PRNG 闭包。判定依据是引用关系，不是长度、随机程度或函数名。|

### 哪些“常见捷径”这次没有用

- 没有识别到足以可靠归属某个混淆器产品／版本的水印；没有据变量名或 Base94 就报品牌。
- 没有执行脚本后 Hook `loadstring`、dump VM、打印寄存器或抓取运行轨迹。
- 没有直接套用 Lua 原生 opcode 编号、原生 `.luac` 头、固定 32 位指令布局。
- 没有仅凭大数组相邻就认定它们是字节码：最大的 12,310 字符片段实际承载解析器源码，连续小数组承载另一个加载器，业务载荷却只有 113 字节。

---

## 4. 实际还原出的东西

### 4.1 分层结构与外层控制流

```text
原文件：返回一个函数调用的结果
  └─ 大表 b : vH()
      ├─ 环境／原生函数／元表诱饵检查
      └─ tB → My 阈值树状态机
          3  cB    建立预期字节和 16047
          4  KM    恢复 C[1]
          7  Ra    恢复 C[2]
          8  Mu    恢复 C[3]
          10 uc    恢复 C[4]
          12 YO    恢复 C[5]
          19..211  94 个业务为空的状态处理器
          217 [92] 安装 UB 到 C[6]，此时尚未调用 UB
          218 iH   安装调用包装到 C[7]，并覆盖 b.iH
          231 Rx   字节和与原生来源检查，写 C[9]
          233 jN   要求 C[9]==C[8]，否则永久循环
          236 dl   返回 C[7]
  └─ 文件末尾的 (...) 调用 C[7]
      └─ UB(C[1]..C[2]..C[3]..C[4]..C[5])
          ├─ 解出片段加载器、43 个 handlers 和字节码解析器
          ├─ 解析 1 个函数原型，补丁 W[1].W[6]=42
          └─ 执行 VM 的结构在源码中可见；本次没有实际执行
```

完整的 105 个状态节点、输入区间和返回边保存在 `recovered/outer_cfg.json`。

### 4.2 五段外层明文与密钥

先去掉每个字的掩码：

```text
word[i] = (b[67][i] - (3448791227 + i*3668954807)) mod 2^32
```

取每个 word 的四个小端字节，再按段截取。第二层递推模数均为 `2^28`：

```text
初始 x = 192143432
一次 b[5] 调用：
    x = ( 4302625*x +  25673739) mod 2^28
    x = (26599703*x + 180079477) mod 2^28
    x = (30867989*x +  10222205) mod 2^28
    输出 x，并保存给下次使用

段内第 j 个字节（j 从 1 开始）：
    plain[j] = masked[j] XOR ((x+j) mod 256)
```

|段|生成的 x|有效长度|
|---:|---:|---:|
|1|262786479|39|
|2|26315172|39|
|3|117049539|39|
|4|224507872|39|
|5|98978423|38|

恢复的五段如下，使用 JSON 字符串转义表示，反斜杠需要按 JSON 还原：

```json
{{OUTER_PARTS}}
```

合计 194 个实际字符，字节和 16047。完整拼接文本另存 `recovered/payload.base94.txt`。

### 4.3 Base94 字母表、转义与各层流参数

以下是按值 `0..93` 排列的字母表，代码块中的反斜杠是一个实际字符，不是转义：

```text
{{ALPHABET}}
```

转义表：

```text
YF2pQ → 双引号 "       Y9dUk → 单引号 '
YEPED → %              YqKls → 空格
Ym75y → $              YOiTX → !
YQHFh → ~              Y62CR → #
Ybt7z → }              Y1nQc → &
```

每五个字母表值按 94 进制累计成整数，再输出其四个小端字节。业务载体首四字节为有效长度 `113`，之后是 113 字节数据，最后 3 字节为载体填充。

除外层 XOR 外，下表所列流掩码均使用 `state=(mul*state+add) mod 2^28`，再减去 `state%256`：

|用途|初始状态／种子规则|mul|add|
|---|---|---:|---:|
|94 项字母表|63870079|25620441|64642393|
|60 字节转义映射|240363193|17542905|2358669|
|2,686 字节加载器|65493484，条件是原生检查返回 `[C]`|5974577|4366095|
|每个处理器／解析器片段|`(243153214 + w*1698984) mod 2^28`|18271769|39726727|
|业务载荷|`(61885231+(fi-1)*204531683) mod 2^28`|23937053|12276273|
|常量池|本原型 seed=22229|15352387|175672043|

业务载荷在第 64 字节之后重置流种子，追加的是前 64 字节**密文**的 31 倍滚动哈希。本例该哈希为 `225835152`。这些算法与常量都来自样本自身，没有外部密钥。

### 4.4 被恢复的中间源码

已逐字节提取：

- `stage1_loader.decoded.lua.txt`：2,686 字节。
- `bytecode_parser.decoded.lua.txt`：9,848 字节。
- `handlers/op_00.decoded.lua.txt` ～ `op_42.decoded.lua.txt`：43 个文件，合计 22,623 字节。
- `all_handlers.annotated.md`：去掉重复 ABI 声明并替换 `MS[n]` 后的可读汇总；它是注释整理版，原始提取文本仍保留。

例如，三个关键处理器的原始主体是：

```lua
-- handler 17: GETGLOBAL
V[a + 1] = G[C[b + 1]]

-- handler 37: LOADK
local t = C[b + 1]; V[a + 1] = t

-- handler 10: MOVE
local t = V[b + 1]; V[a + 1] = t
```

这里的英文助记符是我依据语义添加的，不是声称恢复了生成器原本的 opcode 名称。

### 4.5 函数原型与全部常量槽

```text
原型数          = 1
nregs           = 3
nparams         = 0
vararg          = 0
upsrc           = {}
S               = {8,9,4}
nconst          = 6
constant_seed   = 22229
constant_length = 43
ncode           = 6
operand_checksum= 411152
```

|序列化常量槽|类型标签|还原值|业务是否引用|
|---:|---:|---|---|
|1|4|659975|否|
|2|4|12|是，唯一调用实参|
|3|3|`"print"`|是，全局键|
|4|0|nil|否|
|5|0|nil|否|
|6|4|581310|否|

nil 是序列化格式中的槽位值；解析到 Lua 表后，相应键不存在，不能用 Lua 的 `#C` 推出“常量只有 3 项”。这里的 6 来自显式的 `nconst`。

数字编码恢复公式：

```text
m  = 无符号变长整数
kp = 无符号变长整数
value = m * 2^(floor(kp/2)-2048)
若 kp 为奇数：value = -value
```

字符串用两字节小端长度，随后是原始字节。

常量池独立解密后的全部 43 字节：

```text
{{CONSTANT_HEX}}
```

### 4.6 解密后的二进制与分节

下面是业务载荷去掉其外层流掩码后的完整 113 字节。**其中偏移 18～60 的常量区仍是单独加密的存储形式**；该区的明文已在上一节完整给出。

```text
{{PAYLOAD_HEX}}
```

区间采用 0 起始、右端不包含：

|标签|字节区间|意义|
|---:|---|---|
|237 / 0xED|[0,2)|upvalue 描述，数量 0|
|118 / 0x76|[2,6)|寄存器／形参／vararg 元信息|
|221 / 0xDD|[6,10)|常量区种子|
|82 / 0x52|[10,15)|逻辑到物理寄存器槽映射|
|86 / 0x56|[15,61)|常量数、常量区长度、43 字节常量数据|
|191 / 0xBF|[61,113)|指令数、opcode 列及四个操作数列|

样本没有出现可跳过的 `230/0xE6` 诱饵节；解析器有这种能力不等于本次载荷使用了它。

### 4.7 全部指令及操作数

原始存储列：

```text
W  = {17,37,23,10,16,14}
SA = {29222,35180,1,23073,0,40696}
SB = {62818,46739,0,49105,0,37715}
SC = {2,1,1,3,0,29473}
SD = {8,3,1,7,0,57104}
```

VM 运行前把 `W[6]` 从 14 改为 42。真实 handler 参数顺序为 **`a=SD,b=SC,c=SA,d=SB`**：

{{INSTRUCTION_TABLE}}

对 CALL 的寻址展开：

```text
a = 1；base = a+1 = 2
f   = V[S[2]] = V[9] = ENV["print"]
arg = V[S[3]] = V[4] = 12
固定实参数 b=1；不追加 vararg（d=0）；结果槽参数 c=1
```

这个 CALL 实现至少填充一个结果槽；若函数返回更多值，它还会写入其他映射槽／溢出槽。本例随后仅复制第一个槽，且不再使用。标准 `print` 通常无返回值，此时该槽被填成 nil；业务结论不依赖于真的运行 `print`。

静态 CFG：

```text
PC1 → PC2 → PC3 → PC4 → PC5 → EXIT
PC6：不可达；14 和补丁后的 42 都是 NOP
```

因此最小等价业务代码为：

```lua
print(12)
```

更贴近中间形式、但局部名由我添加的表达：

```lua
local unused_result = print(12)
return
```

第二段不证明原作者确实写了这个局部变量；它只是表达了字节码中“保存返回值但未使用”的事实。

### 4.8 VM 的统一 ABI 与完整操作码语义表

处理器参数为 `handler(E,a,b,c,d)`，上下文槽如下：

```text
E[ 1] V：物理寄存器表       E[ 2] C：常量表
E[ 3] S：逻辑槽映射         E[ 4] O：多返回值溢出槽
E[ 5] G：全局环境           E[ 6] vargs：变长实参表
E[ 7] vargc：变长实参数量    E[ 8] ups：upvalue 引用
E[ 9] makefn：构造闭包      E[10] mget：取得元方法
E[11] resolve_call          E[12] callcap：调用并捕获所有返回值
E[13] HAS_LEN_META          E[14] string.char
E[15] math.floor            E[16] error
E[17] type                  E[18] getmetatable
E[19] rawget                E[20] rawset
E[21] unpack                E[22] MS：类型／元方法／报错字符串
额外字段 E.lb / E.ln：上一调用的返回值基址与数量
```

下表 `V[x]` 是物理槽，涉及 CALL、RETURN 等的逻辑槽需经过 `S`。算术／比较／表操作多有显式元方法回退，不能仅按运算符一行概括所有边缘行为；完整代码见处理器附件。

{{OPCODE_TABLE}}

四个调用类 opcode `15/19/23/31` 在主解释器中另有内联实现；它们对应 `j[27]/j[40]/j[29]/j[39]`。解出的同号 handler 与这些内联路径语义对应，不能因主循环只有四个长分支就推断 VM 只有四条指令。

闭包引用也已识别：

```text
upsrc >= 49152：转引用父函数的 upvalue 对象
upsrc >= 32768：从父寄存器取一个容器，使用其第 1 槽
其余：引用父寄存器表及经 S 映射后的物理索引
```

本次原型的 `upsrc={}`，没有嵌套业务原型；这些是 VM 能力，不是本业务实际做了闭包操作。

### 4.9 已恢复的 VM 内部字符串表

以下 29 项是处理器实际使用的 `MS/kI` 表，不包含外层一般 API 名称：

```json
{{VM_STRINGS}}
```

另外恢复出的保护相关字符串包括：`debug`、`info`、`loadstring`、`load`、`[C]`、`l`、`s`、`number`、`string`、`function`、`error`、`warn`、`unpack`、`gmatch`、`:(%d+):`。完整前置 `GS` 表在 `vm_strings.json`。

### 4.10 防御／反调试／反篡改逻辑

#### A. 入口宿主与内建函数检查

1. 捕获 `type/pcall/xpcall/error/rawget/rawset/getmetatable/setmetatable` 等引用并检查其类型。
2. 用局部表验证 `rawset/rawget` 存储、读取、删除函数值的行为，并要求 `pcall(error)` 失败。
3. 比较 `getfenv()` 返回环境中的 `print/warn` 与捕获的引用，检查环境形状及函数类型。
4. 在可用时测试 `unpack({},0,64)` 是否被正常接受。

这些属于 API 行为／环境一致性探测。只根据名称说它们“检测到了调试器”是不准确的。

#### B. 源码位置与原生来源检查

- 对故意执行 `(nil)[1]` 的局部函数查询 `debug.info(f,"l")`、`debug.info(f,"s")`。
- 通过 `xpcall` 获取错误字符串，用 `:(%d+):` 逐个提取行号，保留最后一个，再要求它等于查询到的定义行。
- 要求这个 Lua 函数的来源不是 `[C]`。
- 对 `error`、可用的 `loadstring/load` 检查来源是否为 `[C]`，以识别非原生替换／包装。
- 外层部分检查在 API 不存在时会跳过；**内部 `debug.info(loadstring,"s")=="[C]"` 是继续解码的必需门禁**，缺少这些能力并不意味着整脚本仍能正常运行。

#### C. 元方法诱饵

- 如果 `newproxy` 可用，建立代理对象，把 `__tostring/__concat/__call` 设为陷阱。
- 建立带保护性 `__metatable` 的普通表，把 `__tostring/__concat/__iter` 设为陷阱。
- 同时检查 `getmetatable` 是否返回保护值，把这些对象保存在局部列表 `oc4` 中。

陷阱函数设置失败标志，随后进入不可正常结束的错误路径。它们可能干扰自动记录器、对象格式化器或会主动遍历／调用元方法的检查工具；**代码没有证明所有调试器都会触发它们**。

#### D. 失败方式

多处失败路径直接 `while true do end`。入口统一失败函数还有：

```lua
local counter = 0
local stop -- nil
repeat
    counter += 1
    _ENV = { error = captured_error }
until counter == stop
```

正常数字计数器不会等于 nil，因此是挂起陷阱；若没有走入该分支，后续还有 `(nil)[1]` 的错误兜底。这不是一般的“报错退出”。

#### E. 完整性与解码耦合

- 外层五段字符串字节和必须等于 16047。
- `[C]` 的哈希 89621 耦合进加载器密钥，失败会先挂起。
- 常量区消费位置必须恰好等于 `cstart+csl`。
- 四列操作数和必须等于 `OS[1]=411152`；它包括未使用字段中的填充数，但不包括 opcode 列。
- 前 64 字节密文哈希用于重置后半段载荷掩码，使简单替换头部也会影响尾部解码。

#### F. 不是同一性质的几个机制

- `li=hash(tostring({}))` 是动态路由盐，但两端同加可静态消去，不是不可恢复的环境密钥。
- `HAS_LEN_META` 用一个 `__len` 返回 99 的表探测兼容性，然后决定是否显式调用 `__len`；这段本身不是失败即挂起的反调试检查。
- 第六 opcode 补丁、空表假循环、算术填充、未引用的 `mp` 属于反静态干扰；并非额外业务功能。
- 没有在已恢复的业务路径中发现网络访问、文件读写或外部命令调用。保护层的动态编译载入均以样本内数据为输入；本次没有执行这些编译调用。

### 4.11 静态校验结果

|检查|结果|
|---|---|
|外层 194 字符字节和|16047，吻合|
|43 个 handler + 1 个解析器的有效密文字节重编码|44/44 逐字节吻合|
|Base94 编码、长度头、转义反向还原|与原始五段拼接逐字符吻合|
|113 字节载荷流变换往返|逐字节吻合|
|43 字节常量池流变换往返|逐字节吻合|
|分节解析是否消耗全部 113 字节|是，无尾随未解释数据|
|四列操作数和|411152，与原解析器 OS[1] 吻合|
|第六 opcode 补丁值|42；原 14 与新 42 均 NOP|
|空表假循环的 JT|0，循环不可进入|

这些是数据与格式一致性证据，不是运行测试，也不是密码学意义上的真实性认证。详细数值和文件哈希见 `verification.json`。

---

## 5. 卡在哪：实际阻碍、修正与没有恢复的内容

### 已解决的阻碍

1. **作用域遮蔽：**第一版辅助提取器误取到外层 `local u=0`，而不是 UB 内部字母表种子，导致 94 个字符不再互异。把匹配约束到 UB 作用域后解决。
2. **又一个同名局部量：**首次搜 `_t` 命中了外层无效条件值，而不是加载器流状态；受限算术解析器拒绝该表达式。修正作用域后再解码。
3. **稠密寄存器假设不成立：**曾检查 `S` 是否是 `1..nregs` 的排列，结果失败；回读 `V[a+1]` 与 `V[S[...]]` 后确认它是稀疏物理位置 `{8,9,4}`。没有为让结果通过而改写源数据。
4. **纯语法匹配问题：**状态树归一化首次未兼容谓词与 `then` 之间的空白；修正词法模式后得到 104 个可证明的包装和 105 个路由叶子。

这些失败均发生在**我自己的静态辅助脚本**里，不是样本或解码代码的运行结果。

### 没有恢复／不能唯一恢复

- **原始源码逐字文本、变量名、注释、排版。** 表示中没有足够信息。
- **`12` 的编译前表达式。** 它可能原本就是字面值，也可能是上游常量折叠结果；这里不猜。
- **原作者是否显式保存 print 的返回值。** 有一条 MOVE，但该值未被使用；编译器临时变量与源码局部变量不能据此唯一对应。
- **混淆器的品牌、版本和具体生成配置。** 未见可靠的独有签名；本报告只认定实际观察到的机制。
- **任意宿主上的真实检查结果、真实 `tostring({})` 地址。** 用户禁止运行，未观察这些值；后者也因代数抵消不影响业务还原。
- **通用 VM 与所有 Lua/Luau 边缘语义完全等价。** 43 个处理器主体已读出，但没有执行兼容性测试。它包含手工元方法分派、`__call` 为表时的特殊路径及使用 `#args` 计算 vararg 数量等实现细节，不能自动假定完全符合某版原生语言规范。
- **与业务无关的巨型随机填充的生成来源。** 已依据引用切片排除，不再对每个随机字节追求不存在证据支持的“隐藏含义”。

**没有剩余的密钥障碍阻止恢复本次业务载荷。** 停止点主要是不可逆的源码信息丢失与禁止动态验证的边界，而不是没找到 VM。

---

## 6. 自评：确定还原与推测分开

以下百分比是主观置信度，不是统计测量：

|结论|置信度|证据／边界|
|---|---:|---|
|样本使用 Luau 方言|>99%|明确的 if-expression、复合赋值与 continue|
|五段载体、常量表和六条指令字段|>99%|按源码公式恢复，长度／校验和／反向编码均吻合|
|正常通过保护后业务等价于 `print(12)`|约 99%|完整五条可达指令的数据流；不是仅字符串猜测|
|43 个 handler 的核心指令语义|约 98%|全部正文恢复；复杂元方法边缘行为未动态测试|
|主要反 Hook／反调试机制的静态含义|约 95%|可见条件、API 调用及失败路径；未观察具体宿主触发|
|原源码确实有某个 `local unused_result`|不作此结论|只知道返回值保存后未使用|
|指定混淆器品牌／版本|未知|证据不足|

- **分析步数：14 个主要步骤。** 另有上节列出的 4 个辅助提取问题修正；没有把脚本报错隐去包装成一次成功。
- **确定恢复：**外层正常路由、五段数据、所有必要解码参数、转义字母表、44 个中间源码片段、1 个原型、6 个常量槽、6 条指令、业务 CFG、具体保护条件。
- **有意识不作推断：**源码变量名、数字的原始表达式、产品归属、在某宿主实际输出的日志与反调试触发记录。
- **最有效的突破点：**`C[1]..C[5]` 的反向切片、成功路径强制 `[C]` 的密钥约束、`Cl` 三元组消费者、统一 22 槽 handler ABI，以及 `SD/SC/SA/SB` 与 `S` 的寻址核对。

### 交付物索引

- `lua_static_report.md`：本报告。
- `recovered/recovered_logic.lua`：只保留等价业务逻辑的人工重建。
- `recovered/recovered_ir.lua.txt`：解释性中间表示，明确标注为非可执行 IR。
- `recovered/disassembly.md`、`bytecode.csv`、`prototype.json`：完整指令、常量和结构。
- `recovered/static_constants.json`、`vm_strings.json`：解码常量及字符串。
- `recovered/stage1_loader.decoded.lua.txt`、`bytecode_parser.decoded.lua.txt`、`handlers/`：全部提取源码，以 `.lua.txt` 保存，避免与建议执行的程序混淆。
- `recovered/all_handlers.annotated.md`、`opcode_semantics.json`：完整处理器语义。
- `recovered/outer_cfg.json`、`segments_manifest.json`、`source_anchors.json`：路由、片段定位与证据锚点。
- `recovered/verification.json`：静态校验结果与哈希。
- `analysis/*.py`：本次静态辅助工具；复现顺序见 `README.md`。其中没有 Lua 执行器或 VM 执行循环。
