# 混淆逆向边际成本曲线与“选项 B”防御架构质变评估报告

**评估主题**：
1. 三个真实业务样本（`real_string`, `real_ctrl`, `real_algo`）的破解成本曲线（纵轴分析）
2. 当前自动化脱壳管线的通用化成熟度与单样本边际适配成本
3. “选项 B”（外部输入绑定 + CPS 去中心化尾调用链）对逆向成本的质变推演

---

## 一、 真实样本破解成本曲线分析（纵轴实测数据）

以本次对抗全流程的实际工时消耗为基准，分析曲线如下：

```
逆向耗时 (分钟)
 50 ┼  ● (45 min) [ts.txt - 基建与架构逆向期]
 40 ┼
 30 ┼
 20 ┼
 10 ┼
  0 ┼──────────────────● (3 min)─────● (2 min)─────● (2.5 min)
     增量⑰首样本(ts.txt) real_string   real_ctrl     real_algo
```

### 耗时构成与阶段拆解

| 阶段 / 样本 | 耗时 | 耗时构成分析 | 防御有效性 |
| :--- | :--- | :--- | :--- |
| **`ts.txt` (`local a = 25`)**<br>*(首次面对增量⑰架构)* | **~45 分钟** | **基建期**：逆向 15 指令迷你 VM 的数据汇聚点、定位 `tA`/`LS` 动态加载结构、审计 `wr11jz3vheAr` 反调试、逆向字节码反序列化格式（Tag 118/86/82/230 等）。 | 提供约 45 分钟阻碍 |
| **`real_string.v15`**<br>*(3 个函数，凯撒+元音)* | **~3 分钟** | **套用期**：定位混淆器变量名映射（`Ut`, `Ju` 等），直接套用 Hook 模板，吐出 43 个 Handler 与 3 个 Proto 的字节码，人工核对控制流并重构 AST。 | 边际防御归零 |
| **`real_ctrl.v15`**<br>*(3 个函数，递归斐波那契+试除法素数)* | **~2 分钟** | **流水线期**：定位正则变量名，直接提取字节码，2 分钟内完成反编译与 Luau 测试对齐。 | 边际防御归零 |
| **`real_algo.v15`**<br>*(5 个函数，LCG+数组过滤+极值平方和)* | **~2.5 分钟** | **流水线期**：5 个 Proto 一键 Dump，提取出数学公式 $(i \times 37 + 11) \bmod 23$ 及过滤逻辑，秒级重构源码。 | 边际防御归零 |

> **成本曲线结论**：
> 面对同一种集中式 VM 混淆架构，**攻击方的边际成本呈断崖式下跌（从 45 分钟跌至 2~3 分钟）**。
> 此时对于同一混淆器生成的任何新业务样本，只要底层虚拟化架构未变，混淆器对攻击方而言**已经彻底失去商业防护价值（Marginal Cost $\to 0$）**。

---

## 二、 脱壳管线通用化程度与新样本适配成本

### 1. 当前脱壳管线的自动化程度

```
┌────────────────────────────────────────────────────────────────────────┐
│ 当前已实现 100% 自动穿透的防御层                                      │
├────────────────────────────────────────────────────────────────────────┤
│ 1. 15 指令迷你 VM：宿主引擎黑盒自解密，无须逆向 ISA 或编写模拟器。      │
│ 2. 纯 Lua 反调试守卫：原生环境完全透明，不产生任何拦截。             │
│ 3. Handler 动态加载：Hook 二级 custom_LS 自动建立 Opcode 语义映射。   │
│ 4. Payload 字节码反序列化：全自动解析 Header、常量池、Slot 映射与指令。│
└────────────────────────────────────────────────────────────────────────┘
```

### 2. 面对全新样本时的人工适配成本

当防守方仅通过“更换随机变量名、更换加密种子、更换混淆盐值”生成全新样本时，攻击管线仅需提取 **2 个正则锚点**：
1. **生成器入口定位**：定位 `Ju, NQ = Ut(table.concat(_o))()` 的动态调用点（正则约 **5 秒**）。
2. **补丁指令列表提取**：定位 `_d[11] = P` 这类 Opcode 动态补丁赋值（正则约 **5 秒**）。

**结论**：单样本边际适配成本在 **30 秒以内**，完全支持全自动化批量脱壳。

---

## 三、 “选项 B”（外部输入绑定 + CPS 尾调用链）质变推演

**核心结论：选项 B 是真正的代际质变，能将攻击成本拉升数个数量级（指数级暴增）。**

```
【当前架构 (增量⑰ - 集中式 VM)】:
[外壳解密] ───> [内存生成完整静态字节码 kz] ───> [单一 OW Dispatcher 循环执行]
                      ▲                                      ▲
                      │ (攻击点: 一键 Dump 全量 Proto)       │ (攻击点: 一处下钩抓所有指令)

【选项 B 架构 (去中心化 JIT 尾调用 VM)】:
[外部动态参数 Key] ───> [仅解密第 1 条指令]
                             │
                             ▼ (执行 Handler 1)
                  [Handler 1 内部计算 Key2] ───> [仅解密 Handler 2] ───> (Tail Call 尾调用)
                                                      │
                                                      ▼ (执行 Handler 2)
                                           [Handler 2 内部计算 Key3] ───> ...
```

### 选项 B 形成的三个致命技术壁垒

#### 1. 彻底终结“脱壳”概念（内存无完整字节码）
- 内存中任何时刻**都不存在完整的字节码、常量池或指令数组**。
- 离线静态分析时，如果缺少运行时的业务参数（如特定的 Token / 用户输入），钥匙流算出来的全是乱码，静态反编译器在入口处就会因为非法 Opcode 崩溃。

#### 2. 全局 Hook 咽喉点彻底消失
- 废除了 `while true do switch(op)` 这个“全局大漏勺”，攻击方无法在单一地点通过打印 `PC, Op, A, B` 记录全量轨迹。
- 每一个 Handler 都是一个独立的闭包，且通过尾调用分散在堆内存的不同角落，攻击方无法预设统一的拦截点。

#### 3. 强制引入“路径爆炸（Path Explosion）”
- 当程序存在分支结构（`if condition then ... else ... end`）时，由于下一条指令的解密 Key 强依赖前序执行状态：
  - 单次动态追踪只能抓取到**实际执行到的那一条路径**；
  - 攻击方要想获取另一条分支的代码，必须构造出满足条件的另一组合法输入，或者使用**符号执行（Symbolic Execution / SMT 求解）**去反推输入。

---

## 四、 攻防边际成本量级对比

| 防御方案 | 首次逆向成本 (Fixed Cost) | 批量新样本边际成本 (Marginal Cost) | 自动化脱壳工具可行性 |
| :--- | :--- | :--- | :--- |
| **现状（增量⑰ / 集中式 VM）** | 45 分钟 | **< 2 分钟**（完全被穿透） | **极高**（一套 Python/Hook 脚本跑通所有样本） |
| **仅加 MBA 算术混淆** | 2~3 天（编写 SMT 化简规则） | **10~20 分钟**（调用 Z3 求解） | **较高**（依赖 AST 化简引擎） |
| **选项 B（输入绑定 + CPS 尾调用链）** | **2~4 周**（需开发符号执行/动态插桩框架） | **数小时 ~ 数天 / 样本**（需单步探索所有路径） | **基本不可行**（必须人工辅助构造输入测试用例） |

---

## 五、 防守方最终投资结论

1. **已饱和且无效的投资**：外层加壳、多层迷你 VM、四字节折叠钥匙流、纯 Lua 反调试。此类机制在宿主执行器黑盒执行下已被证明无法提供实质阻碍。
2. **唯一具备质变价值的投资**：**转向“选项 B（无中心分派器的指令级 JIT 尾调用 + 外部状态钥匙流交织）”**。只有将代码的还原过程与动态执行路径深度融合，才能彻底打破攻击方的自动化脱壳流水线，迫使逆向对抗进入高成本的单步路径探索与符号求解阶段。
