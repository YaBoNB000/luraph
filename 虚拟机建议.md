**建议1

把所有虚拟机指令，比如5.1的38条写成38个文件，每个文件返回固定的解释器代码，用哪个就返回哪个，这样比较好维护，比如
返回   "{{STK}}[{}] = ({} ~= 0); if {} ~= 0 then {{PC}} = {{PC}} + 1 end",
        

**进阶
每个指令随机分配几个不同的格式，每次虚拟机格式不同，每次生成打乱顺序指令重排

**建议2

虚拟机主循环用while true do ，每条指令用if 条件 then 真指令 else 假指令 end 或者 if 条件 then 假指令 else 真指令，每次生成完全随机，每个指令都延伸出分支

**进阶
在每个指令或者加密解密运行之前，必须使用类似
local erstring="wtf,son"
local a = newproxy()
local is_userdata = type(a) == "userdata"
local addr = tostring(a)
local hex = addr:match("0x(%x+)")
if is_userdata and hex and hex:sub(1, 3) == "000" then
    error(erstring,0)
end
这种的环境检测，创建一个anti文件夹，里面存放所有的反调试，比如anti1.rs anti3. rs，每次随机加入，而且最好不要直接放在明显的位置，最好是分成几个function，再一起调用，这样可以防止单调试，最好和上面的建议2一起使用

**建议3

防止hook loadstring这种的重要函数，使用这种检测
local function isNative(func)
    local info = debug.getinfo(func, "S")
    return info and info.what == "C" and info.source == "=[C]"
end

local function get()
    local candidates = {
        loadstring,
        (getrenv and getrenv() or _ENV or _G)["loadstring"],
        (getrenv and getrenv() or _ENV or _G)["load"],
        load
    }
    for _, f in ipairs(candidates) do
        if type(f) == "function" and isNative(f) then
            return f
        end
    end
    return nil
end
local ploadstring=get()

而且不要检测一次，在每次使用load loadstring的时候都进行检测

**建议4

关于字节码或者其他加密数据，不是加密越强就越安全，而且保持一定强度并且隐藏最终输出，不然直接print(dec(加密数据))那就没用了，字节码最好提高混乱度，也可以通过xor直接显示二进制乱码(可选)，而不是base或者16进制转义，加密可以考虑字节级别，这样强度比较高而且熵值也高

**进阶

在字节码解密 最后运行时 加密数据解密时一定特别注意，不要直接一步到位，这样非常不安全，最好分步运行以及加入反调试，保证不会被一步破解

**建议5(慎选)

使用bit库来储存字节码数据，(只限roblox)，难度较高，但是对于静态分析有提升

**总结

所有操作都不要一步到位，这样结构清晰而且容易破解，最好每个步骤都分为几个function，其中加入反调试，每个部分通过才运行，一起结合才能输出
