[package]
name = "luraph-rs"
version = "0.2.0"
edition = "2021"
description = "Commercial-grade Lua 5.1 / Luau obfuscator with custom VM"

[dependencies]
# std-only on purpose: the sandbox cannot reach crates.io (see HANDOFF.md)

[profile.release]
opt-level = 3
lto = true
