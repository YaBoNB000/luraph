# 混淆示例（所有常用语法）

`tests/cases/` 里每个语料文件经过**默认管线**的输出。增量㉕ 起默认
即 v15 架构；增量㉖ 起产品线收敛为 **Luau-only**（Lua 5.1 目标不再
支持）——因此每个语料只有一个示例：`*.v15.luau.lua`，它就是裸调用
`luraph-rs in.lua out.lua` 的产品形态（零参数直接可运行）。

**对照方法**：`tests/cases/X.lua` 是原始代码，`X.v15.luau.lua` 是混淆
结果——两者在 Luau 解释器上运行输出完全一致（`tests/run_tests.sh` /
`tests/run_presets.sh` / `tests/multiseed.sh` 矩阵验证全绿；minify 默认
开，示例文件为单行紧凑形态）。生成命令：`bash tests/gen_examples.sh`
（固定 `--seed 42 --preset v15`，同构建字节级确定）。

**增量㊻（2026-09-19，R018 报告剩余建议落地：P2 + P0-1 + P1 收窄）**：
① AV 取值源改异构三源（新表地址串/新闭包地址串/os.clock 折字节）+
每构建变形（去单一 `tostring({})` 识别锚点，R018 P0-1）；② pb 双消费
前移——hqi 段表解掩种子同时绑定原生性探针与环境值折叠（P1 收窄）；
③ 语料新增 `multi_proto.lua`（P2 多原型）。模板变动 ⇒ 本目录示例已由
㊻ 构建重新生成（38 例）。正向验证见 `tests/faithful_stub_test.sh`
（含 pb 双消费存在性断言）。
**增量㊺（2026-09-19，绑定折叠前移——环境值绑定首次真实生效）**：双
根治（ef1 读前写 + 镜像错位）+ ef1 前移揉进 hqi 段表解掩种子（引导期
第一个解码步骤即环境绑定）。**未绑定态零混入实证**：本增量不改变未绑
定产物的 RNG 流，本目录全部示例与 ㊹ 构建**逐字节一致**（`diff -r`
实证），无需重新生成。正向验证见 `tests/faithful_stub_test.sh`。
**增量㊹（2026-09-19，R018 报告 P0-2 收口）**：BSS 的 loadstring+调用
搬进 HBOOT 掩码层——解码态源码不再落地可见层（R018 报告的 gsub 手术
攻击面关闭）；本目录示例已由 ㊹ 构建重新生成。
**增量㊸（2026-09-19，环境绑定去中心化+语义强化）**：`--bind-env`
绑定机制升级到样本级「难绕过」形态——环境 API 进模块表高位槽（混原语
槽族）、引导槽位间接调用、折叠拆段散布、语义探针（typeof/tostring）、
错环境/篡改统一静默毒药（无报错无信号）。绑定态示例不入本目录（只在
Roblox 可载）。
**增量㊷（2026-09-19，显在性复查）**：全词普查确认保护色彩词簇清零（debug/print/warn/newproxy/loadstring/os/_ENV 全 0，样本同口径）；EV 改 `getfenv(1)` 环境克隆；本目录示例已由 ㊷ 构建重新生成。
**增量㊶（2026-09-19，保护代码隐藏专项）**：对照样本 15 形态整改——
守卫/蜜罐/闸门全部搬进 HBOOT 掩码层（可见层死循环/蜜罐词/码表/拼串
表痕迹清零），环境名构造改掩码派生；本目录示例已由 ㊶ 构建重新生成。
**增量㊵（2026-09-19，运行期钥匙每运行化）**：块钥匙基（RPK1..5）与
BSS 实参混入每运行必变的 AV（新表地址折叠），静态重放运行期阶段中间态
一次性；本目录示例已由 ㊵ 构建重新生成。
**增量㊳（2026-09-18，计时守卫复通）**：计时守卫上线回合曾由 ㊳ 构建重新生成
——RT 每 128 分派步采样 `os.clock`，两连窗慢于基线 100 倍 ⇒ 钥匙流投毒
静默损坏（反调试/反 trace；触发路径与误报面验证见
`tests/defense_audit.sh` D12）。旧 `*.5.1.lua` / `*.vm.5.1.lua` 遗留产物
（㉖ 前形态，曾被误提交）已从本目录清除。

**㊱ 引导层悬挂标识符修复（2026-09-17，无参数）**：收口 ㉟ 遗留的稀有
崩溃（~0.3% 种子×语料，「attempt to call a table value」）。根因 =
symtab 块作用域缺失 → 模板 CLK 悬挂引用被 mangle 改名 → v15 scaffold
随机填充参数名撞名捕获寄存器值。修复 = 模板外层显式 CLK + symtab 给
Do/While/Repeat 开作用域 + scaffold 名字池排除解释器自由标识符。

**㉟ 碎片增量链压缩（2026-09-17 起，默认开启，无参数）**：自研压缩——
HBOOT 解码链即压缩链，每片对「此前已解码源码字典」做 4-gram 贪心 LZ 增量
编码（字面段/字典回引两种操作流），字典逐片生长；加密层不变。

**㉞ 一趟流水融合（2026-09-16 起，默认开启，无参数）**：自研压缩——
chunk handler 原两趟流水（解码→concat→二趟 XOR）融合成趟（XOR 钥匙
只随字节序号线性变化、无状态推进），配 string.byte 多返回值 + string.char
多参数整词入表；五形态变体正交组合不变。

**㉝ 词表长字符串化（2026-09-16 起，默认开启，无参数）**：字节码词表从
十进制数字槽数组改 5 字符/词连续区 base-94 长字符串（解码纯 Horner 无查
表），掩码/分片机制不变，载荷数据密度 ~2.3 倍。

**㉛ 引导期掩码三件套（2026-09-15 起，默认开启，无参数）**：种子探针项
改分裂乘积直参形（减法零化形清除）；hqi 段表与 metavm 程序字加性 LCG
掩码落盘、引导期即解（钥匙每构建重抽）——静态侦察不再白拿段表结构与
可直模拟的明文程序字。

**㉚ B-2 源码自校验哈希链（2026-09-14 起，默认开启，无参数）**：全部
v15 输出内嵌「哈希即钥匙」——HBOOT 引导在 loadstring 每片之前对其明文
源码走双车道链（链头 = HBOOT 自身源码），每片钥匙种子揉入此前全部源码
链值；链终值在 RT 碎片内做双独立归零约束揉进指令块/常量池钥匙流。源码
完整 ⇒ 偏移为 0 原样跑；任何一层被改 ⇒ 程序照跑、解密内容全错（无比较
语句/死循环可补）。每片带 14–22B 随机尾注（长度熵 + 自由填充位）。

**v15 结构同族示例（路线 A，2026-08-27 起，全语料覆盖）**：全部 **36 个
语料**（27 共享 + 9 `luau_*`，㉜ 起含多赋值交换回归语料 `swap_multi`）
各有 `--preset v15` 输出 `<case>.v15.luau.lua`
（**仅 Luau/Roblox 目标**）：3 行形态（头注释 + 空行 +
`return setmetatable({...},{}):XX()(...);`）——模块表 = 65 原语数字槽
+ 2 诱饵 LCG 工厂 + 具名常量混存 + CPS 引导骨架（初始化器/顶层机/
`continue` 叶循环/单字符 staging handler 链/算术助手，handler 数随原型数
线性扩展）。执行 = CPS 分发（每指令 `H[OC.*]` handler + Call 系内联 +
真 TCO，深尾递归不溢栈）；解码 = 显式状态机 + LCG 密钥流分块（解码点
无密钥常量）；**字节码操作数散布**（阶段 A：寄存器/常量/upvalue 每函数
随机槽，随机面全在载体编码 blob 内）。验收：`luau-compile` 语法检测
30/30 通过 + 运行输出与原语料逐一一致；对照标准 =
`docs/v15-structural-parity-plan.md` 的 32 条指纹 —— **已 32/32 全部
达成，且对全部 30 语料 × 5 种子稳定成立**（结构战役增量 E1–E5：复合
赋值折叠/宽参数/if 表达式/内联 -128 阶梯/RC+HB 长串/字符串池隐匿/
规模地板）。

**单行赋值演示样本**：`local_a.lua`（源 = `local a = 25`）+
`local_a.v15.luau.lua`（`--preset v15` 输出）——最小可运行的对照样本
（此前为 `print6` → `print12`，现为无副作用的局部赋值）。用于快速
肉眼核对 3 行形态与运行行为（`local` 未被使用，运行无输出、退出码 0）。

**增量⑲ 起 v15 执行架构 = CPS 去中心化尾调用链 + 蹦床**（2026-09-07，
对抗 R006 壁垒②）：中心 `while true do fetch dispatch end` 已废除——每条
指令的分派逻辑在各自加密 handler 尾部的**尾链附言**里（预算 ≤16 指令/链，
耗尽解链回落蹦床、轮转加密重入碎片从 `E.pc` 续跑）；Call 家族拆「链相位
暂存 + 执行器碎片浅层调用」两相。可见 `run()`/makefn 闭包只剩无操作码语义
的蹦床循环（`E.b`/`E.callx`/`E.done` 驱动）。Luau 无正确尾调用（官方移除），
蹦床预算即栈深闸门；递归深度实测 8000+ 可用（语料需求 5000）。

**增量㉑ 环境绑定（`--bind-env roblox`，2026-09-08）**：可选开关（仅
v15）。把目标运行时 API（`Vector3.new`/`Vector2.new`/`task.defer`）嵌进
解释器原语槽——产物只在目标运行时（Roblox）可加载，通用分析沙箱在加载期
即死；未绑定产物零参数直接可运行，行为不变。绑定态产物不进通用运行等价
矩阵（`tests/env_bind_test.sh` 单测：语法校验 + 加载失败验证）。

**增量㉒ B-2 碎片即用即毁（2026-09-08）**：v15 原型**加密集散**——
每次调用经常驻解码碎片按帧解出全新流/常量表，帧退即毁；闭包只捕获密文
原型。任意时刻内存只有密文 + 活帧碎片（R006 壁垒①）。对示例产物无可见
形态影响，运行开销 ≈ +2%（每调用解码一帧）。

**增量㉓ 解码窗口入密（2026-09-10，R008 回合复盘）**：㉒ 的编码动作与
Nop 自改写原本在可见 boot 触碰解码态原型表（文本手术即可转储）；㉓ 把
解析→自改写→重编码→链原型树全部合进 BSS 加密碎片，可见层只见编码态
树根交接。攻击者要拿解码态必须先完整破译引导链（每构建重建）。

**增量㉔ 守卫钥匙化 + 环境值绑定（2026-09-10，R008 报告实证路径）**：
① loadstring 原生性探测废除布尔标志/死循环，结果折进元钥匙流（补标志
无目标可打）；② `--bind-env roblox` 升级为值绑定——引导期用每构建随机
参数调 `Vector3.new`/`Vector2.new`，分量随机系数折叠揉进钥匙流，哑桩
折出垃圾即错钥死亡（桩须忠实实现构造语义）。新增语料 `token_mix.lua`
（交错混合+校验和+26 进制编码，与 bind_gate 不同构，防源程序复用污染）。

**增量㉗ 运行时入密（2026-09-10，Suda 回合）**：newE/makefn/run+蹦床/
DDEC 整体打包为加密运行时碎片（wire 208），可见层只剩不透明函数值装配
（`RUN = RTFRAG(...)`）——可见文本不再有任何一行触碰解码数据（Suda 轮
三个实测文本探针点全部消失）。

**B-3 状态链式按块解码（2026-09-10）**：指令流按基本块编码，块钥匙含
boot 期随机会话盐（静态文件无任何可推钥匙的材料）；运行时按块即需即解进
帧私有窗口 + 块缓存——批量解码器消亡，文件级/单次转储级/跨运行重放级
解码全灭。

**[㊴ 退役] 增量⑱ 输入绑定/激活门**：曾以 `--bind-key` 把引导链门控到
变长参递送的激活值上；因与「混淆后直接可运行」冲突，㊴ 起功能/测试
（`bind_gate_test.sh`）/绑定态示例全部移除。原靶标程序（凯撒位移 +
滚动折叠哈希）作为普通真实逻辑语料保留，更名 `real_shift.lua`，示例
`real_shift.v15.luau.lua` 零参数直接可运行。移除经字节级验证：其余全部
未绑定产物与移除前逐字节一致。

**真实逻辑样本（2026-09-06，转向「抬成本」验证）**：极限小样本
（`local a = 25` 类）已不能反映真实攻防，改用**带输入、有真实逻辑**
的样本对照验证（也作为交付攻击方的靶标）：
- `real_string.lua` — 字符串处理（Caesar 密码 + 元音统计），函数/循环/
  条件分支/`string.byte/char`/`table.concat`
- `real_algo.lua` — 表算法（数据生成/过滤/平方和聚合/求最大值），
  多函数/循环/表构造与索引
- `real_ctrl.lua` — 递归 + 记忆化 + 控制流（fib 记忆化 + 素数判定），
  真递归/上值捕获/`while` 循环
三个样本均支持变长参数输入（`...`），无参时用内置默认值（矩阵走无参
默认路径，跨 5.1/Luau 确定性一致；**注：luau CLI 不转发命令行参数
给 `...`，lua51 转发**，带参路径仅作交互演示、不入矩阵）。每个样本的
`--preset v15` 输出经轮廓指纹 32/32 + 安全指纹 5/5（增量⑳ 把 S5 口径
校准到 P4 形态后首次全绿）+ 运行等价验证。

**反调试 guard**：全部档位均带环境完整性 guard（`guard.rs`：全局函数
完整性 / `debug.info` 行号探针 / 负键读写回环 / canary 绊线等，篡改
环境 → 静默挂起）。非 v15 档 = mangle 后的前奏 IIFE；**v15 档 =
CHAR 编码版**（全部字符串走 `GS[k]=string.char(码…)` 数值重建，注入
FC 入口机头部，顶层 3 行样本形态与 32/32 指纹零破坏）。`--no-guard`
关闭。

**输出纯度**：所有示例均为纯 ASCII（2026-08-29 修复了 v15 档载体
blob 重打印时的 UTF-8 直通泄漏：转义产生 ≥0x80 字节的字符串一律按
`\ddd` 重转义）——密文/密钥流/字节码等二进制串
全量 `\ddd` 转义（`Expr::Str.is_binary`），不会泄露随机高字节构成的
可读 Unicode（如繁体字）；用户可读字符串在 L2 开启时同样是加密形态。

**重新生成**：

```bash
cd luraph-rs
CARGO_NET_OFFLINE=true /home/user/luraph/.tools/bin/cargo build --release
tests/gen_examples.sh
```

> 注意：`gen_examples.sh` 会先 `rm -f examples/*.lua` 再从 `tests/cases/`
> 重建。`real_*` 样本已入语料会自动重建；**手工补充的样本（如
> `local_a`）会被清掉，需重新生成**。

## 当前已实现的混淆（M1–M5 全开）

- **L1 名称混淆**：所有局部变量/参数/循环变量/local function 名 → 随机名
  （短/中/长混合风格；避开关键字、程序用到的全局名；隐式 `self` 保持固定名）
- **L1 minify**：token 感知单行压缩（默认开）——输出为单行紧凑形态，只在
  危险边界（标识符黏合 / `--` 成注释 / `1..2` 成坏浮点）保留空格；
  `--no-minify` 可得到缩进形态
- **L2 字符串加密**：
  - 所有字符串字面量 → 运行时解密调用 `dec(chunk1, chunk2, chunk3)`
  - 密码：加性密钥流 `enc[i] = (byte + key[i%24] + i) mod 256`（5.1 无位运算也能跑；
    双方言 `%` 均为 floor 语义——已实测）
  - 密钥拆成 3 段字面量 + 运行时展开为字节表（输出中无连续密钥）
  - 解密器/密钥表变量名同样随机化
- **L7 反篡改**（M3）：金丝雀自检 + 密文校验和（篡改→静默死循环）+
  错误行号重映射 + 时间陷阱
- **L5 整体加密**（M3）：全部 pass 后的整个程序 → 加性密钥流密文
  （3~5 块字面量 + 运行时解密器 + `loadstring(DEC(...))()`）——输出中
  只剩容器，无任何程序结构/明文
- **L4 数值**（M3）：数字字面量 → 随机项和/差或 `a*b+c` 积式（浮点仅恒等
  包裹）；状态机 ID、索引、常量全部不可明文搜索
- **L3 控制流**（M2）：
  - **junk**：函数体顶部注入无副作用算术垃圾块（只碰新局部变量）+
    恒真透明谓词 if（`b*b >= 0`）
  - **CFG 扁平化**：每个函数体 → `while true` + 随机状态 ID + 乱序分支的
    状态机（无 goto 形态）；**循环是嵌套子状态机**（循环体独立分派，
    循环变量/体局部变量每轮 fresh，闭包捕获语义与原生 for 一致）；
    跨分支/跨闭包引用的 local 提升到机器顶部

- **L6 VM 随机面**（M5）：SoA 平行数组（opcode / 四条操作数流分存）+
  完整 7/14/21-bit varint + base-94 载体与 10 特殊字符 token 转义 +
  每构建随机的解码枢纽/状态元组顺序 + 帧入场原语数字槽解包。
  `--vm` 输出经 `luac51 -l` 看不到用户结构。
- **指令拆文件 + 一指令多形态**（建议1）：42 条指令的解释器代码各在
  `src/vmgen/handlers/` 的独立文件里；每条指令带 2~3 个语义等价形态，
  每构建随机各选一形，分派叶序 / handler 定义序 / 内联调用链序每次
  生成打乱——同一份源码、不同种子，解释器指令排布与文本均不同。
- **常量加密 + 动态内联**（安全战役 P1）：字节码常量池逐字节密钥流
  掩码（每函数种子 + 每构建 LCG），数字常量以 dyadic 对存储（无数字
  文本），≤5 字节短串经 MkStr 指令内联进操作数流——静态脱壳拿不到
  任何用户常量（`tests/security_fingerprint.py` 攻击脚本验收）。
- **字节码不规则化**（安全战役 P2）：函数 blob 拆成带 tag 的节
  （含诱饵节）每构建随机乱序，计数字段全 varint，整体再叠位置掩码
  流，闲置操作数填随机值——无固定偏移结构，脱壳后仍是密文。
- **解释器 handler 加密**（安全战役 P3a，v15 档）：42 个指令
  handler 体不以代码出现——环境参数化闭包源经 LCG 掩码 + base-94
  打包存进长字符串数字槽，引导期复检 `loadstring` 原生性
  （`GFE(0)["loadstring"]`，被 hook 即静默陷阱）后解码重建分派表；
  跳转指令返回信号表，帧簿记走每帧环境。静态输出里搜不到任何指令
  语义片段。
- **素材去物化**（安全战役 P3b，v15 档）：opcode 映射表不再有名字
  字面量（引导期从掩码数对重建，分派按指令序号位置访问）；字节码
  字表拆成小数字槽阵列并按词位置掩码；密钥流生成器变形。解码表
  （字母表/令牌）与样本同族保留轮廓，但内容层已加密，仅凭解码表
  无法重建程序——安全指纹五项全绿。
- **诱饵分发点**（安全战役 P3c，v15 档）：死段内每构建随机 1~3 个
  永不执行的死 fetch 环，向样本的多分发点形态靠拢，制造「哪个环是
  真的」问题。轮廓 32 条 + 安全 5 项双指纹门禁，见
  `tests/v15_fingerprint.py` 与 `tests/security_fingerprint.py`。
- **防御代码隐藏**（安全战役 P4，v15 档）：所有防御/校验代码不用时
  看不出是什么——复检目标名（loadstring/debug/info/[C] 等）全部由
  打乱码表+序表运行时拼接构造并经 `getfenv(0)[...]` 索引；完整性
  校验字节求和走数字槽原语、循环形态逐实例轮换。输出里不存在任何
  防御特征明文。

## 预设（M6）

根目录 `README.md` 有完整用法。示例按**默认 high** 生成（与 `--preset high`
逐字节一致）。VM 三件示例等价于 `--preset vm`。五档对照矩阵：
`tests/run_presets.sh`（405 项）。性能：`docs/performance.md`。
v15 档（路线 A 结构同族）为独立发射管线，示例见上节，验收以
`tests/v15_fingerprint.py` 对照 `samples/luraph15.txt`。

## 全项防御审计（增量㊲）

`tests/defense_audit.sh`：13 个防御点逐一正/负向实测（干净运行等价、
钥匙去字面化、结构/安全指纹、密文完整性与字节码防篡改的 30 点真篡改、
激活门、环境绑定、反挂钩闸、反指纹闸、蜜罐结构、计时守卫、
LZ 往返自检）。改防御相关发射面后应跑一次：`bash tests/defense_audit.sh`。
计时守卫（㊳ 复通）：RT 每 128 步采样 `os.clock`，两连窗慢于基线
100 倍 ⇒ 钥匙流投毒静默损坏——反调试/反 trace；审计 D12 对触发路径
做确定性实证（抽出守卫 + 合成 tracer 时钟）。
