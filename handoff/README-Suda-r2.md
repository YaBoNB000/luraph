# Suda 复测（R011）交付说明 —— 增量㉗ 运行时入密

> 2026-09-10 · 上一轮 `Suda_target.luau.lua`（seed 1024，㉓ 架构）被三个
> 文本探针破解（探针全部插在**可见运行时表面**）。本轮交付同源复测靶标，
> 验证增量㉗ 的加固效果。

## 靶标

**`Suda_r2_target.luau.lua`**（131 KB，`print("Suda")` 同源，seed 2048）

- 与 `Suda_target.luau.lua` 逻辑完全相同（零信息量，纯防护层考验）；
- 架构差异 = 增量㉗：`newE`/`makefn`/`run`+蹦床/`DDEC` 整体入密为
  运行时碎片，可见层只剩不透明函数值装配——上一轮的三个探针插入点
  （entry 的 `run(MPF,...)` 前 / 可见 `newE` 的 `DDEC` 调用点后 / 可见
  蹦床 `CT` 表包装）在产物中**已不存在**（可见文本零触碰解码数据）；
- 零参数直接可运行，输出 `Suda`。

## 任务口径（沿用上轮）

穿透保护层、提取内部字节码与常量池、还原等价源码，并给出真实脱壳证据
（不允许仅凭运行观察交答案）。方法不限，静态+动态结合。

## 对照价值

同逻辑、同口径、唯一变量 = 防护架构（㉓ → ㉗）。本轮的破解成本变化
即为增量㉗ 的实测防御收益。若仍能破解，请在报告中说明新的切入点位于
哪一层（可见装配局部 / 碎片内部 / 其他），以便我方定位下一轮加固方向。
