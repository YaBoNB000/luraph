# Suda Luau VM 保护验收：脱壳分析报告

## 结论

已通过实际运行时插桩取得根原型、规范化后的六条虚拟指令列和常量池，并据此还原出 [restored_source.luau](restored_source.luau)。还原版本与原靶标在标准 Luau CLI 下均输出 `Suda`。

本结论不是依据输出手写：常量池中实际提取出了字符串 `print` 与 `Suda`，且它们位于同一无子原型的 VM 载荷中；完整关键摘录见 [vm_extraction.txt](vm_extraction.txt)。

## 可复现基线

动态运行（官方 `luau.exe`）的原靶标：

```text
luau.exe D:\QQSave\target.luau.lua.txt
Suda
```

还原版：

```text
luau.exe restored_source.luau
Suda
```

## 穿透路径与证据

| 层 | 手段 | 实际证据 | 结论 |
|---|---|---|---|
| 外层装配 | 静态 | 文件以 `return setmetatable({...},{}):GB()(...);` 收束；表内把原生 API 以数字索引保存 | 外层是表驱动控制流平坦化装配器 |
| 反分析 | 静态+动态 | `GB` 内检查 `debug.info`、`loadstring`/`load`、环境表及 `newproxy`；失败分支进入不可终止/错误路径 | 存在环境指纹与反篡改噪声，但并未阻止局部、同进程探针 |
| 五段密文 | 静态 | `kH` 构造 `C[1]..C[2]..C[3]..C[4]..C[5]`，随后调用 `C[6]` | 明确的装配汇合点；这些片段是引导输入而非源码文本 |
| 运行时 VM 建立 | 静态 | `C[6]=GJ`；`GJ` 构造状态机，生成 `qQ`，最终调用 `bL(qQ,jC,{},vU,0)` | `qQ` 是可截获的根原型，`bL` 是执行入口 |
| 原型/常量提取 | 动态 | 在 `bL(qQ,...)` 前递归枚举 `qQ`；在 `mV` 的 `BH(yP,...)` 后枚举规范化结果 | 已得到真实结构、操作数列及常量池 |
| 语义还原 | 动态+静态 | 根原型无参数、无子原型；规范化指令恰为六槽；常量槽 `[4]=print`、`[6]=Suda`；执行从 `pc=1` 进入并输出 `Suda` | 源码为一次无参数的 `print` 调用 |

## 插桩说明

探针仅写入工作副本 `work/probe_payload.luau`，原附件未修改。

1. 在 `GJ` 尾部、`return bL(qQ,jC,{},vU,0)` 之前：递归打印 `qQ`，目的为获取反序列化后的根原型。
2. 在 `mV` 中 `BH(yP,...)` 返回之后：打印六个规范化列，目的为取得执行器实际读取的指令列与常量表。
3. 给四个 `Aa` 调度相位加透明包装：记录首次调度 `pc`、opcode tag 和前三个操作数，目的为确认该结构确实被 VM 执行，而不是闲置解码数据。

所有探针保留在 `work/make_probe.ps1`，可重新生成工作副本；提取结果固定在 [vm_extraction.txt](vm_extraction.txt)。

## 提取内容

根原型的 `nparams=0`、`upsrc={}`、`k={}`，因此没有参数、上值或子原型。原始字段和 BH 规范化后的六个指令槽均已保存于提取文件。特别是，规范化常量表为：

```text
[3] = 302907
[4] = "print"
[6] = "Suda"
```

对应的 opcode tag 列为 `[11, 2, 42, 23, 10, 28]`。操作数列仍是该 VM 指令编码，不应误称为标准 Luau bytecode；其完整数值摘录见提取文件。

## 还原依据

该原型唯一可见字符串常量是全局名称 `print` 与实参 `Suda`，无子原型、无上值、无参数；运行时 VM 对该原型从首指令开始执行并产生 `Suda`。据此得到：

```lua
print("Suda")
```

这是从脱壳产物的常量池和指令执行路径推得，不是从原始输出单独猜测。

## 防御评估

- 高风险：解密后原型在 Lua 表 `qQ` 中存活，且到 `bL` 前有稳定单点；局部源码插桩可直接枚举整个对象图。
- 高风险：常量（包括 `print`、`Suda`）在 `BH` 返回后以明文 Lua string 存在，未做到按需短生命周期清除。
- 中风险：反调试主要针对全局 API 与调试栈特征；它没有保护闭包内部的本地表，也没有防止在同一闭包路径插入观测代码。
- 中风险：控制流平坦化和噪声算术增大阅读成本，但不会改变“反序列化后必须形成可执行数据结构”的观察面。

建议：缩短明文常量与完整原型的存活时间；将敏感常量按使用点解密并即时擦除；为完整性校验覆盖关键闭包代码和结构化载荷；不要把所有层级原型一次性实体化为普通可遍历 table。即便如此，客户端侧可执行逻辑仍应假定可被授权分析者观测，关键决策应服务端化。

## 置信度与边界

- 高置信度：根原型、常量池、六条规范化指令槽、`print("Suda")` 的行为和输出；均有动态探针结果支持。
- 高置信度：五段拼接、`C[6]=GJ`、`qQ` 到 `bL` 的静态引导链；直接来自可见层代码。
- 未作推测：未给六个自定义 opcode 强行命名为标准 Luau 指令，也未声称提取到了标准 Luau 编译器字节码；该靶标实际执行的是自定义 VM 格式。
